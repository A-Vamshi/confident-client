// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: transformers).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

export enum TransformerLanguage {
  PYTHON = "PYTHON",
}

export interface TransformerCodeDefinition {
  code: string;
  language: TransformerLanguage;
}

export interface CreateTransformerRequest {
  name: string;
  description?: string | null;
  codeDefinition: TransformerCodeDefinition;
}

export interface TestTransformerCodeRequest {
  inputData?: unknown;
}

export interface Transformer {
  id: string;
  name: string;
  description: string | null;
  createdAt: string;
  updatedAt: string;
  codeDefinition: TransformerCodeDefinition | null;
}

export interface TransformerCodeRunFailure {
  success: boolean;
  error: string | null;
  reason: string | null;
  verboseLogs: string | null;
}

export interface TransformerCodeRunSuccess {
  success: boolean;
  output?: unknown;
  verboseLogs: string | null;
}

export interface TransformerSummary {
  id: string;
  name: string;
  description: string | null;
  createdAt: string;
  updatedAt: string;
}

export interface TransformerList {
  transformers: TransformerSummary[];
  totalTransformers: number;
  page: number;
  pageSize: number;
}

export interface TransformerRef {
  id: string;
}

export interface UpdateTransformerRequest {
  name?: string;
  description?: string | null;
  codeDefinition?: TransformerCodeDefinition;
}

export type TransformerCodeRunResult =
  TransformerCodeRunSuccess | TransformerCodeRunFailure;

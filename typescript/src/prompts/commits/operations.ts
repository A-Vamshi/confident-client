// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: prompts).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

import { HttpMethods } from "../../api";
import { Endpoints } from "../../endpoints";
import { BranchesOperations } from "../branches/operations";
import { Prompt, PromptCommitList } from "../types";

export class CommitsOperations extends BranchesOperations {
  /**
   * List Prompt Commits
   *
   * Lists the commits of the prompt, newest first. Pass `branch` to list only
   * the commits reachable from that branch.
   *
   * @param promptId The unique id of the prompt.
   * @param branch The name of the branch to read from. Defaults to `main` when
   *   omitted.
   */
  async getPromptCommits(
    promptId: string,
    branch?: string,
  ): Promise<PromptCommitList> {
    return this.api.sendRequest<PromptCommitList>(
      HttpMethods.GET,
      Endpoints.PROMPT_COMMITS_ENDPOINT,
      { urlParams: { promptId }, params: { branch } },
    );
  }

  /**
   * Pull Prompt by Commit
   *
   * Retrieves the prompt commit with the given `hash`. The commit is looked up
   * on `main` unless a `branch` is given.
   *
   * @param promptId The unique id of the prompt.
   * @param hash The hash of the commit to pull.
   * @param branch The name of the branch to read from. Defaults to `main` when
   *   omitted.
   */
  async getPromptByCommit(
    promptId: string,
    hash: string,
    branch?: string,
  ): Promise<Prompt> {
    return this.api.sendRequest<Prompt>(
      HttpMethods.GET,
      Endpoints.PROMPT_COMMIT_ENDPOINT,
      { urlParams: { promptId, hash }, params: { branch } },
    );
  }
}

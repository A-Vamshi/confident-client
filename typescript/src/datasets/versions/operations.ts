// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: datasets).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

import { HttpMethods } from "../../api";
import { Endpoints } from "../../endpoints";
import { GoldensOperations } from "../goldens/operations";
import { CreateDatasetVersionResult, DatasetVersionList } from "../types";

export class VersionsOperations extends GoldensOperations {
  /**
   * List Dataset Versions
   *
   * Lists every version of the dataset, newest first. Requires the Team plan or
   * above.
   *
   * @param datasetId The unique id of the dataset.
   */
  async getDatasetVersions(datasetId: string): Promise<DatasetVersionList> {
    return this.api.sendRequest<DatasetVersionList>(
      HttpMethods.GET,
      Endpoints.DATASET_VERSIONS_ENDPOINT,
      { urlParams: { datasetId } },
    );
  }

  /**
   * Create Dataset Version
   *
   * Snapshots the current state of the dataset as a new immutable version. The
   * first version takes over every unversioned golden; each later version
   * copies every golden, tag and custom column from the previous version.
   * Requires the Team plan or above.
   *
   * @param datasetId The unique id of the dataset.
   */
  async createDatasetVersion(
    datasetId: string,
  ): Promise<CreateDatasetVersionResult> {
    return this.api.sendRequest<CreateDatasetVersionResult>(
      HttpMethods.POST,
      Endpoints.DATASET_VERSIONS_ENDPOINT,
      { urlParams: { datasetId } },
    );
  }
}

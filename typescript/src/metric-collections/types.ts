// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: metric-collections).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

import { ModelProvider } from "../common/types";

export interface MetricRef {
  name: string;
}

export interface MetricSettingConfig {
  metric: MetricRef;
  activated?: boolean;
  threshold?: number;
  includeReason?: boolean;
  strictMode?: boolean;
  sampleRate?: number;
  evaluationModelProvider?: ModelProvider | null;
  evaluationModelName?: string | null;
}

export interface CreateMetricCollectionRequest {
  name: string;
  multiTurn?: boolean;
  metricsSettings?: MetricSettingConfig[];
  sampleRate?: number;
  inputTransformerId?: string | null;
  outputTransformerId?: string | null;
}

export interface MetricSummary {
  id: number;
  name: string;
}

export interface MetricSetting {
  metric: MetricSummary;
  activated: boolean;
  threshold: number;
  includeReason: boolean;
  strictMode: boolean;
  sampleRate: number;
  evaluationModelProvider: ModelProvider | null;
  evaluationModelName: string | null;
}

export interface MetricCollection {
  id: string;
  name: string;
  multiTurn: boolean;
  sampleRate: number;
  inputTransformerId: string | null;
  outputTransformerId: string | null;
  metricsSettings: MetricSetting[];
}

export interface MetricCollectionList {
  metricCollections: MetricCollection[];
}

export interface MetricCollectionRef {
  id: string;
}

export interface UpdateMetricCollectionRequest {
  name?: string;
  metricsSettings?: MetricSettingConfig[];
  sampleRate?: number;
  inputTransformerId?: string | null;
  outputTransformerId?: string | null;
}

// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: attack-methods).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

import { Level } from "../common/types";

export enum AttackParameterType {
  STRING = "string",
  INTEGER = "integer",
  FLOAT = "float",
  BOOLEAN = "boolean",
  ENUM = "enum",
  JSON = "json",
}

export interface AttackParameter {
  type: AttackParameterType;
  required?: boolean;
  description?: string;
  options?: string[];
  default?: unknown;
}

export interface AttackMethod {
  id: string;
  name: string;
  description: string | null;
  multiTurn: boolean;
  exploitability: Level | null;
  configurable: boolean;
  parameters: Record<string, AttackParameter> | null;
}

export interface AttackMethodSummary {
  id: string;
  name: string;
  description: string | null;
  multiTurn: boolean;
  exploitability: Level | null;
  configurable: boolean;
}

export interface AttackMethodList {
  attackMethods: AttackMethodSummary[];
  totalAttackMethods: number;
  page: number;
  pageSize: number;
}

export interface AttackMethodRef {
  id: string;
}

export interface UpdateAttackMethodRequest {
  parameters: Record<string, unknown>;
}

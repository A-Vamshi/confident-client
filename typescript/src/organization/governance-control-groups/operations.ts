// @generated by scripts/generate_sdk.py from
// packages/shared/openapi/openapi.yml (tag: organization).
// Do not edit by hand — change the route in confident-cloud
// and regenerate.

import { HttpMethods } from "../../api";
import { Endpoints } from "../../endpoints";
import { AuditLogsExportsOperations } from "../audit-logs-exports/operations";
import {
  GovernanceControlGroup,
  GovernanceControlGroupList,
  GovernanceControlGroupRef,
} from "../types";

export class GovernanceControlGroupsOperations extends AuditLogsExportsOperations {
  /**
   * List Governance Control Groups
   *
   * Lists your organization's governance control groups, newest created first,
   * with each group's controls counted rather than named. A group is a label
   * for organizing controls and does not itself govern anything — what a
   * control gates comes from the governance policies holding it, never from its
   * group.
   *
   * @param page The page to return. Defaults to 1.
   * @param pageSize The number of control groups per page, at most 100.
   *   Defaults to 25.
   */
  async listGovernanceControlGroups(
    page?: number,
    pageSize?: number,
  ): Promise<GovernanceControlGroupList> {
    return this.api.sendRequest<GovernanceControlGroupList>(
      HttpMethods.GET,
      Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_GROUPS_ENDPOINT,
      { params: { page, pageSize } },
    );
  }

  /**
   * Create Governance Control Group
   *
   * Creates a governance control group and returns its id. The group starts
   * empty, since which controls belong to it is set on each control rather than
   * here, and putting a control in a group has no effect on which projects it
   * gates or when it is assessed.
   *
   * @param name The name of the control group, unique within your organization.
   * @param description What the group collects. Send null to leave it unset.
   */
  async createGovernanceControlGroup(
    name: string,
    description?: string | null,
  ): Promise<GovernanceControlGroupRef> {
    return this.api.sendRequest<GovernanceControlGroupRef>(
      HttpMethods.POST,
      Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_GROUPS_ENDPOINT,
      { body: { name, description } },
    );
  }

  /**
   * Get Governance Control Group
   *
   * Retrieves a single governance control group with the controls inside it,
   * which the list endpoint reports only as a count. Each control is named
   * rather than resolved — retrieve a control by id for its health, its policy
   * membership and its definition.
   *
   * @param controlGroupId The id of the governance control group.
   */
  async getGovernanceControlGroup(
    controlGroupId: string,
  ): Promise<GovernanceControlGroup> {
    return this.api.sendRequest<GovernanceControlGroup>(
      HttpMethods.GET,
      Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_GROUP_ENDPOINT,
      { urlParams: { controlGroupId } },
    );
  }

  /**
   * Delete Governance Control Group
   *
   * Permanently deletes a governance control group. The controls that were in
   * it are not deleted and become ungrouped, and they keep gating exactly the
   * projects they gated before, since a group never scoped that. **Warning:**
   * This action cannot be undone.
   *
   * @param controlGroupId The id of the governance control group.
   */
  async deleteGovernanceControlGroup(
    controlGroupId: string,
  ): Promise<GovernanceControlGroupRef> {
    return this.api.sendRequest<GovernanceControlGroupRef>(
      HttpMethods.DELETE,
      Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_GROUP_ENDPOINT,
      { urlParams: { controlGroupId } },
    );
  }
}

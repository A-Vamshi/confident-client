# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: scheduled-alerts).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Literal, Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import (
    FilterSet,
    ScheduleIntervalUnit,
    ScheduleRecurrenceType,
)
from confidentai.endpoints import Endpoints
from confidentai.scheduled_alerts.types import (
    AlertDataModel,
    AlertSeverity,
    AlertThresholdSettings,
    CreateScheduledAlertRequest,
    ScheduledAlert,
    ScheduledAlertList,
    ScheduledAlertRef,
    UpdateScheduledAlertRequest,
)


class ScheduledAlertsClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list(
        self,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None,
        data_model: Optional[AlertDataModel] = None,
        enabled: Optional[Literal["true", "false"]] = None
    ) -> ScheduledAlertList:
        """List Scheduled Alerts

        Lists the scheduled alerts in your Confident AI project one page at a
        time, ordered by name. Each alert is returned as a summary row; retrieve
        one by id for its aggregation, filters, threshold, severity and run
        history.

        Args:
            page: The page of scheduled alerts to return. Defaults to 1.
            page_size: The number of scheduled alerts per page, at most 100.
                Defaults to 25.
            data_model: Returns only alerts measuring this kind of item. Omit to
                return all of them.
            enabled: Returns only alerts whose schedule is running when `true`,
                or only the paused ones when `false`. Omit to return both.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.SCHEDULED_ALERTS_ENDPOINT,
            response_schema=ScheduledAlertList,
            query={
                "page": page,
                "pageSize": page_size,
                "dataModel": data_model,
                "enabled": enabled,
            },
        )

    async def a_list(
        self,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None,
        data_model: Optional[AlertDataModel] = None,
        enabled: Optional[Literal["true", "false"]] = None
    ) -> ScheduledAlertList:
        """List Scheduled Alerts

        Lists the scheduled alerts in your Confident AI project one page at a
        time, ordered by name. Each alert is returned as a summary row; retrieve
        one by id for its aggregation, filters, threshold, severity and run
        history.

        Args:
            page: The page of scheduled alerts to return. Defaults to 1.
            page_size: The number of scheduled alerts per page, at most 100.
                Defaults to 25.
            data_model: Returns only alerts measuring this kind of item. Omit to
                return all of them.
            enabled: Returns only alerts whose schedule is running when `true`,
                or only the paused ones when `false`. Omit to return both.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.SCHEDULED_ALERTS_ENDPOINT,
            response_schema=ScheduledAlertList,
            query={
                "page": page,
                "pageSize": page_size,
                "dataModel": data_model,
                "enabled": enabled,
            },
        )

    def create(
        self,
        name: str,
        data_model: AlertDataModel,
        aggregation: str,
        threshold_settings: AlertThresholdSettings,
        *,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None,
        description: Optional[str] = None,
        filters: Optional[FilterSet] = None,
        severity: Optional[AlertSeverity] = None,
        enabled: Optional[bool] = None
    ) -> ScheduledAlertRef:
        """Create Scheduled Alert

        Creates an alert that re-runs an aggregate query on a schedule and
        notifies when the result crosses the threshold, and returns its id.
        Notifications are delivered through the project's integrations that have
        alerting enabled for the alert's severity, so an alert in a project with
        no such integration still evaluates but reaches nobody.

        Args:
            name: A name for the alert, shown in the notification.
            aggregation: What to measure, as an aggregation token. Which tokens
                are valid depends on `dataModel`: `TRACE` accepts COUNT,
                ERROR_RATE, PASS_RATE, UNIQUE_END_USERS, UNIQUE_THREADS,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, TOTAL_COST,
                AVG_COST, UNIQUE_METADATA_VALUES; `SPAN` accepts COUNT,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE,
                ERROR_COUNT, INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST,
                INPUT_TOKENS, OUTPUT_TOKENS, TOTAL_TOKENS,
                UNIQUE_METADATA_VALUES; `LLM_SPAN` accepts COUNT, AVG_LATENCY,
                P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT,
                INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST, INPUT_TOKENS,
                OUTPUT_TOKENS, TOTAL_TOKENS, UNIQUE_METADATA_VALUES;
                `AGENT_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `RETRIEVER_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `TOOL_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `CUSTOM_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `THREAD` accepts COUNT, UNIQUE_USERS, UNIQUE_METADATA_VALUES.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
            description: What the alert means and what to do about it, included
                in the notification. Send null to clear it.
            filters: Narrows what the alert measures over, so an alert can watch
                one route rather than the whole project. Send null to clear the
                filters and measure everything.
            enabled: Whether the schedule runs. Defaults to true.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.SCHEDULED_ALERTS_ENDPOINT,
            response_schema=ScheduledAlertRef,
            request_schema=CreateScheduledAlertRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "aggregation": aggregation,
                "thresholdSettings": threshold_settings,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
                "description": description,
                "filters": filters,
                "severity": severity,
                "enabled": enabled,
            },
        )

    async def a_create(
        self,
        name: str,
        data_model: AlertDataModel,
        aggregation: str,
        threshold_settings: AlertThresholdSettings,
        *,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None,
        description: Optional[str] = None,
        filters: Optional[FilterSet] = None,
        severity: Optional[AlertSeverity] = None,
        enabled: Optional[bool] = None
    ) -> ScheduledAlertRef:
        """Create Scheduled Alert

        Creates an alert that re-runs an aggregate query on a schedule and
        notifies when the result crosses the threshold, and returns its id.
        Notifications are delivered through the project's integrations that have
        alerting enabled for the alert's severity, so an alert in a project with
        no such integration still evaluates but reaches nobody.

        Args:
            name: A name for the alert, shown in the notification.
            aggregation: What to measure, as an aggregation token. Which tokens
                are valid depends on `dataModel`: `TRACE` accepts COUNT,
                ERROR_RATE, PASS_RATE, UNIQUE_END_USERS, UNIQUE_THREADS,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, TOTAL_COST,
                AVG_COST, UNIQUE_METADATA_VALUES; `SPAN` accepts COUNT,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE,
                ERROR_COUNT, INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST,
                INPUT_TOKENS, OUTPUT_TOKENS, TOTAL_TOKENS,
                UNIQUE_METADATA_VALUES; `LLM_SPAN` accepts COUNT, AVG_LATENCY,
                P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT,
                INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST, INPUT_TOKENS,
                OUTPUT_TOKENS, TOTAL_TOKENS, UNIQUE_METADATA_VALUES;
                `AGENT_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `RETRIEVER_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `TOOL_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `CUSTOM_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `THREAD` accepts COUNT, UNIQUE_USERS, UNIQUE_METADATA_VALUES.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
            description: What the alert means and what to do about it, included
                in the notification. Send null to clear it.
            filters: Narrows what the alert measures over, so an alert can watch
                one route rather than the whole project. Send null to clear the
                filters and measure everything.
            enabled: Whether the schedule runs. Defaults to true.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.SCHEDULED_ALERTS_ENDPOINT,
            response_schema=ScheduledAlertRef,
            request_schema=CreateScheduledAlertRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "aggregation": aggregation,
                "thresholdSettings": threshold_settings,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
                "description": description,
                "filters": filters,
                "severity": severity,
                "enabled": enabled,
            },
        )

    def get(self, scheduled_alert_id: str) -> ScheduledAlert:
        """Get Scheduled Alert

        Retrieves a scheduled alert by id, with its aggregation, filters,
        threshold, severity and schedule state including how many times it has
        run.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlert,
            path={"scheduledAlertId": scheduled_alert_id},
        )

    async def a_get(self, scheduled_alert_id: str) -> ScheduledAlert:
        """Get Scheduled Alert

        Retrieves a scheduled alert by id, with its aggregation, filters,
        threshold, severity and schedule state including how many times it has
        run.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlert,
            path={"scheduledAlertId": scheduled_alert_id},
        )

    def update(
        self,
        scheduled_alert_id: str,
        *,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None,
        description: Optional[str] = None,
        filters: Optional[FilterSet] = None,
        severity: Optional[AlertSeverity] = None,
        name: Optional[str] = None,
        data_model: Optional[AlertDataModel] = None,
        aggregation: Optional[str] = None,
        threshold_settings: Optional[AlertThresholdSettings] = None,
        enabled: Optional[bool] = None
    ) -> ScheduledAlert:
        """Update Scheduled Alert

        Updates a scheduled alert and returns it. Only the fields you send are
        changed; omitting a field leaves it untouched, and sending null clears
        it. Because each `dataModel` accepts a different set of aggregations,
        send `aggregation` alongside `dataModel` when moving an alert between
        data models.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
            description: What the alert means and what to do about it, included
                in the notification. Send null to clear it.
            filters: Narrows what the alert measures over, so an alert can watch
                one route rather than the whole project. Send null to clear the
                filters and measure everything.
            name: A new name for the alert, shown in the notification.
            aggregation: What to measure, as an aggregation token. Which tokens
                are valid depends on `dataModel`: `TRACE` accepts COUNT,
                ERROR_RATE, PASS_RATE, UNIQUE_END_USERS, UNIQUE_THREADS,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, TOTAL_COST,
                AVG_COST, UNIQUE_METADATA_VALUES; `SPAN` accepts COUNT,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE,
                ERROR_COUNT, INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST,
                INPUT_TOKENS, OUTPUT_TOKENS, TOTAL_TOKENS,
                UNIQUE_METADATA_VALUES; `LLM_SPAN` accepts COUNT, AVG_LATENCY,
                P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT,
                INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST, INPUT_TOKENS,
                OUTPUT_TOKENS, TOTAL_TOKENS, UNIQUE_METADATA_VALUES;
                `AGENT_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `RETRIEVER_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `TOOL_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `CUSTOM_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `THREAD` accepts COUNT, UNIQUE_USERS, UNIQUE_METADATA_VALUES.
            enabled: Whether the schedule runs. An alert whose run limit or end
                date has passed cannot be re-enabled without also moving
                `maxRuns` or `endAt`.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlert,
            request_schema=UpdateScheduledAlertRequest,
            body={
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
                "description": description,
                "filters": filters,
                "severity": severity,
                "name": name,
                "dataModel": data_model,
                "aggregation": aggregation,
                "thresholdSettings": threshold_settings,
                "enabled": enabled,
            },
            path={"scheduledAlertId": scheduled_alert_id},
        )

    async def a_update(
        self,
        scheduled_alert_id: str,
        *,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None,
        description: Optional[str] = None,
        filters: Optional[FilterSet] = None,
        severity: Optional[AlertSeverity] = None,
        name: Optional[str] = None,
        data_model: Optional[AlertDataModel] = None,
        aggregation: Optional[str] = None,
        threshold_settings: Optional[AlertThresholdSettings] = None,
        enabled: Optional[bool] = None
    ) -> ScheduledAlert:
        """Update Scheduled Alert

        Updates a scheduled alert and returns it. Only the fields you send are
        changed; omitting a field leaves it untouched, and sending null clears
        it. Because each `dataModel` accepts a different set of aggregations,
        send `aggregation` alongside `dataModel` when moving an alert between
        data models.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
            description: What the alert means and what to do about it, included
                in the notification. Send null to clear it.
            filters: Narrows what the alert measures over, so an alert can watch
                one route rather than the whole project. Send null to clear the
                filters and measure everything.
            name: A new name for the alert, shown in the notification.
            aggregation: What to measure, as an aggregation token. Which tokens
                are valid depends on `dataModel`: `TRACE` accepts COUNT,
                ERROR_RATE, PASS_RATE, UNIQUE_END_USERS, UNIQUE_THREADS,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, TOTAL_COST,
                AVG_COST, UNIQUE_METADATA_VALUES; `SPAN` accepts COUNT,
                AVG_LATENCY, P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE,
                ERROR_COUNT, INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST,
                INPUT_TOKENS, OUTPUT_TOKENS, TOTAL_TOKENS,
                UNIQUE_METADATA_VALUES; `LLM_SPAN` accepts COUNT, AVG_LATENCY,
                P50_LATENCY, P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT,
                INPUT_COST, OUTPUT_COST, TOTAL_COST, AVG_COST, INPUT_TOKENS,
                OUTPUT_TOKENS, TOTAL_TOKENS, UNIQUE_METADATA_VALUES;
                `AGENT_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `RETRIEVER_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `TOOL_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `CUSTOM_SPAN` accepts COUNT, AVG_LATENCY, P50_LATENCY,
                P90_LATENCY, P99_LATENCY, ERROR_RATE, ERROR_COUNT, INPUT_COST,
                OUTPUT_COST, TOTAL_COST, AVG_COST, UNIQUE_METADATA_VALUES;
                `THREAD` accepts COUNT, UNIQUE_USERS, UNIQUE_METADATA_VALUES.
            enabled: Whether the schedule runs. An alert whose run limit or end
                date has passed cannot be re-enabled without also moving
                `maxRuns` or `endAt`.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlert,
            request_schema=UpdateScheduledAlertRequest,
            body={
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
                "description": description,
                "filters": filters,
                "severity": severity,
                "name": name,
                "dataModel": data_model,
                "aggregation": aggregation,
                "thresholdSettings": threshold_settings,
                "enabled": enabled,
            },
            path={"scheduledAlertId": scheduled_alert_id},
        )

    def delete(self, scheduled_alert_id: str) -> ScheduledAlertRef:
        """Delete Scheduled Alert

        Permanently deletes a scheduled alert and unregisters its next run. To
        stop an alert temporarily, update it with `enabled` set to false
        instead. This action cannot be undone.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlertRef,
            path={"scheduledAlertId": scheduled_alert_id},
        )

    async def a_delete(self, scheduled_alert_id: str) -> ScheduledAlertRef:
        """Delete Scheduled Alert

        Permanently deletes a scheduled alert and unregisters its next run. To
        stop an alert temporarily, update it with `enabled` set to false
        instead. This action cannot be undone.

        Args:
            scheduled_alert_id: The id of the scheduled alert.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.SCHEDULED_ALERT_ENDPOINT,
            response_schema=ScheduledAlertRef,
            path={"scheduledAlertId": scheduled_alert_id},
        )

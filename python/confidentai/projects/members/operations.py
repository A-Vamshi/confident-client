# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: projects).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import MemberRef, UpdateMemberRoleRequest
from confidentai.endpoints import Endpoints
from confidentai.projects.types import ProjectMember, ProjectMemberList


class MembersOperations:
    _api: Api

    def list_project_members(
        self,
        project_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> ProjectMemberList:
        """List Project Members

        Lists the members of one project a page at a time, each with the project
        role that decides what they can do inside it. Project members are drawn
        from the organization's members: someone can belong to the organization
        and not appear here, and being here is what gives them access to this
        project's data. Removing a member from the organization removes them
        from every project in it, this one included.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            page: The page to return. Defaults to 1.
            page_size: The number of members per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.PROJECT_MEMBERS_ENDPOINT,
            response_schema=ProjectMemberList,
            path={"projectId": project_id},
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_project_members(
        self,
        project_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> ProjectMemberList:
        """List Project Members

        Lists the members of one project a page at a time, each with the project
        role that decides what they can do inside it. Project members are drawn
        from the organization's members: someone can belong to the organization
        and not appear here, and being here is what gives them access to this
        project's data. Removing a member from the organization removes them
        from every project in it, this one included.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            page: The page to return. Defaults to 1.
            page_size: The number of members per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.PROJECT_MEMBERS_ENDPOINT,
            response_schema=ProjectMemberList,
            path={"projectId": project_id},
            query={"page": page, "pageSize": page_size},
        )

    def update_project_member_role(
        self, project_id: str, user_id: str, role_id: str
    ) -> ProjectMember:
        """Update Project Member Role

        Replaces a member's role in this project, which changes what they may do
        inside it from their next request onwards. Their organization role and
        their roles in other projects are untouched. Assigning the `Owner` role
        transfers ownership of the project: the member becomes Owner and the
        previous Owner is demoted to `Manager` in the same transaction, which is
        the only way the project Owner's role changes. A role id belonging to
        another project is rejected.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            user_id: The id of the user whose membership of this project to
                change.
            role_id: The id of the role to assign. It must be a Confident AI
                built-in role or one of the roles the organization or project
                owns.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.PROJECT_MEMBER_ENDPOINT,
            response_schema=ProjectMember,
            request_schema=UpdateMemberRoleRequest,
            body={"roleId": role_id},
            path={"projectId": project_id, "userId": user_id},
        )

    async def a_update_project_member_role(
        self, project_id: str, user_id: str, role_id: str
    ) -> ProjectMember:
        """Update Project Member Role

        Replaces a member's role in this project, which changes what they may do
        inside it from their next request onwards. Their organization role and
        their roles in other projects are untouched. Assigning the `Owner` role
        transfers ownership of the project: the member becomes Owner and the
        previous Owner is demoted to `Manager` in the same transaction, which is
        the only way the project Owner's role changes. A role id belonging to
        another project is rejected.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            user_id: The id of the user whose membership of this project to
                change.
            role_id: The id of the role to assign. It must be a Confident AI
                built-in role or one of the roles the organization or project
                owns.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.PROJECT_MEMBER_ENDPOINT,
            response_schema=ProjectMember,
            request_schema=UpdateMemberRoleRequest,
            body={"roleId": role_id},
            path={"projectId": project_id, "userId": user_id},
        )

    def remove_project_member(self, project_id: str, user_id: str) -> MemberRef:
        """Remove Project Member

        Revokes a member's access to this project only: they are disconnected
        from it, their project role is deleted, and any invitation still
        outstanding for their email address on this project is cleared. They
        keep their organization membership and their access to every other
        project, so this does not free up a seat. The project Owner cannot be
        removed, so transfer ownership first. Removal is not reversible through
        this endpoint — invite the same address to the project again to bring
        them back, which returns them with the default role rather than the one
        they had.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            user_id: The id of the user whose membership of this project to
                change.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.PROJECT_MEMBER_ENDPOINT,
            response_schema=MemberRef,
            path={"projectId": project_id, "userId": user_id},
        )

    async def a_remove_project_member(
        self, project_id: str, user_id: str
    ) -> MemberRef:
        """Remove Project Member

        Revokes a member's access to this project only: they are disconnected
        from it, their project role is deleted, and any invitation still
        outstanding for their email address on this project is cleared. They
        keep their organization membership and their access to every other
        project, so this does not free up a seat. The project Owner cannot be
        removed, so transfer ownership first. Removal is not reversible through
        this endpoint — invite the same address to the project again to bring
        them back, which returns them with the default role rather than the one
        they had.

        Args:
            project_id: The id of the project, which must belong to your
                organization.
            user_id: The id of the user whose membership of this project to
                change.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.PROJECT_MEMBER_ENDPOINT,
            response_schema=MemberRef,
            path={"projectId": project_id, "userId": user_id},
        )

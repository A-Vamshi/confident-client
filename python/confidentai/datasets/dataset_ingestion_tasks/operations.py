# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: datasets).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Literal, Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import FilterSet, IngestionDataModel
from confidentai.datasets.types import (
    CreateDatasetIngestionTaskRequest,
    DatasetIngestionTask,
    DatasetIngestionTaskList,
    DatasetIngestionTaskRef,
    UpdateDatasetIngestionTaskRequest,
)
from confidentai.endpoints import Endpoints


class DatasetIngestionTasksOperations:
    _api: Api

    def list_dataset_ingestion_tasks(
        self,
        dataset_id: str,
        *,
        data_model: Optional[Literal["TRACE", "SPAN", "THREAD"]] = None
    ) -> DatasetIngestionTaskList:
        """List Dataset Ingestion Tasks

        Lists the ingestion tasks on the dataset, newest first, as summary rows.
        Get a single task for its full configuration. Requires the Starter plan
        or above.

        Args:
            dataset_id: The unique id of the dataset.
            data_model: Only return tasks harvesting this kind of item. Omit it
                to return all of them.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.DATASET_DATASET_INGESTION_TASKS_ENDPOINT,
            response_schema=DatasetIngestionTaskList,
            path={"datasetId": dataset_id},
            query={"dataModel": data_model},
        )

    async def a_list_dataset_ingestion_tasks(
        self,
        dataset_id: str,
        *,
        data_model: Optional[Literal["TRACE", "SPAN", "THREAD"]] = None
    ) -> DatasetIngestionTaskList:
        """List Dataset Ingestion Tasks

        Lists the ingestion tasks on the dataset, newest first, as summary rows.
        Get a single task for its full configuration. Requires the Starter plan
        or above.

        Args:
            dataset_id: The unique id of the dataset.
            data_model: Only return tasks harvesting this kind of item. Omit it
                to return all of them.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.DATASET_DATASET_INGESTION_TASKS_ENDPOINT,
            response_schema=DatasetIngestionTaskList,
            path={"datasetId": dataset_id},
            query={"dataModel": data_model},
        )

    def create_dataset_ingestion_task(
        self,
        dataset_id: str,
        name: str,
        data_model: IngestionDataModel,
        *,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        sample_rate: Optional[float] = None,
        filters: Optional[FilterSet] = None,
        max_goldens: Optional[int] = None,
        input_transformer_id: Optional[str] = None,
        output_transformer_id: Optional[str] = None,
        include_input: Optional[bool] = None,
        include_actual_output: Optional[bool] = None,
        include_expected_output: Optional[bool] = None,
        include_retrieval_context: Optional[bool] = None,
        include_context: Optional[bool] = None,
        include_tools_called: Optional[bool] = None,
        include_expected_tools: Optional[bool] = None
    ) -> DatasetIngestionTaskRef:
        """Create Dataset Ingestion Task

        Creates a standing rule that harvests matching production traces, spans
        or threads into the dataset as goldens, starting immediately unless
        `enabled` is false, and returns its id. `dataModel` must match the
        dataset: THREAD for multi-turn, TRACE or SPAN for single-turn. Requires
        the Starter plan or above.

        Args:
            dataset_id: The unique id of the dataset.
            name: A name for the task, unique within the dataset.
            description: A note about what the task harvests. Send null to clear
                it.
            enabled: Whether the task runs. Disabling it unschedules the
                harvesting job, and goldens already created are kept. Defaults
                to false.
            sample_rate: The fraction of matching items to ingest, between 0 and
                1. Defaults to 1, all of them.
            max_goldens: The maximum number of goldens this task will ever
                create. Send null to remove the cap.
            input_transformer_id: The id of a transformer that reshapes the
                harvested input before it is stored. Send null to detach it.
            output_transformer_id: The id of a transformer that reshapes the
                harvested output before it is stored. Send null to detach it.
            include_input: Populate the golden's `input` from the harvested
                item. Defaults to true; every other include flag defaults to
                false.
            include_actual_output: Populate the golden's `actualOutput` from the
                harvested item.
            include_expected_output: Populate the golden's `expectedOutput` from
                the harvested item.
            include_retrieval_context: Populate the golden's `retrievalContext`
                from the harvested item.
            include_context: Populate the golden's `context` from the harvested
                item.
            include_tools_called: Populate the golden's `toolsCalled` from the
                harvested item.
            include_expected_tools: Populate the golden's `expectedTools` from
                the harvested item.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.DATASET_DATASET_INGESTION_TASKS_ENDPOINT,
            response_schema=DatasetIngestionTaskRef,
            request_schema=CreateDatasetIngestionTaskRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "description": description,
                "enabled": enabled,
                "sampleRate": sample_rate,
                "filters": filters,
                "maxGoldens": max_goldens,
                "inputTransformerId": input_transformer_id,
                "outputTransformerId": output_transformer_id,
                "includeInput": include_input,
                "includeActualOutput": include_actual_output,
                "includeExpectedOutput": include_expected_output,
                "includeRetrievalContext": include_retrieval_context,
                "includeContext": include_context,
                "includeToolsCalled": include_tools_called,
                "includeExpectedTools": include_expected_tools,
            },
            path={"datasetId": dataset_id},
        )

    async def a_create_dataset_ingestion_task(
        self,
        dataset_id: str,
        name: str,
        data_model: IngestionDataModel,
        *,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        sample_rate: Optional[float] = None,
        filters: Optional[FilterSet] = None,
        max_goldens: Optional[int] = None,
        input_transformer_id: Optional[str] = None,
        output_transformer_id: Optional[str] = None,
        include_input: Optional[bool] = None,
        include_actual_output: Optional[bool] = None,
        include_expected_output: Optional[bool] = None,
        include_retrieval_context: Optional[bool] = None,
        include_context: Optional[bool] = None,
        include_tools_called: Optional[bool] = None,
        include_expected_tools: Optional[bool] = None
    ) -> DatasetIngestionTaskRef:
        """Create Dataset Ingestion Task

        Creates a standing rule that harvests matching production traces, spans
        or threads into the dataset as goldens, starting immediately unless
        `enabled` is false, and returns its id. `dataModel` must match the
        dataset: THREAD for multi-turn, TRACE or SPAN for single-turn. Requires
        the Starter plan or above.

        Args:
            dataset_id: The unique id of the dataset.
            name: A name for the task, unique within the dataset.
            description: A note about what the task harvests. Send null to clear
                it.
            enabled: Whether the task runs. Disabling it unschedules the
                harvesting job, and goldens already created are kept. Defaults
                to false.
            sample_rate: The fraction of matching items to ingest, between 0 and
                1. Defaults to 1, all of them.
            max_goldens: The maximum number of goldens this task will ever
                create. Send null to remove the cap.
            input_transformer_id: The id of a transformer that reshapes the
                harvested input before it is stored. Send null to detach it.
            output_transformer_id: The id of a transformer that reshapes the
                harvested output before it is stored. Send null to detach it.
            include_input: Populate the golden's `input` from the harvested
                item. Defaults to true; every other include flag defaults to
                false.
            include_actual_output: Populate the golden's `actualOutput` from the
                harvested item.
            include_expected_output: Populate the golden's `expectedOutput` from
                the harvested item.
            include_retrieval_context: Populate the golden's `retrievalContext`
                from the harvested item.
            include_context: Populate the golden's `context` from the harvested
                item.
            include_tools_called: Populate the golden's `toolsCalled` from the
                harvested item.
            include_expected_tools: Populate the golden's `expectedTools` from
                the harvested item.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.DATASET_DATASET_INGESTION_TASKS_ENDPOINT,
            response_schema=DatasetIngestionTaskRef,
            request_schema=CreateDatasetIngestionTaskRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "description": description,
                "enabled": enabled,
                "sampleRate": sample_rate,
                "filters": filters,
                "maxGoldens": max_goldens,
                "inputTransformerId": input_transformer_id,
                "outputTransformerId": output_transformer_id,
                "includeInput": include_input,
                "includeActualOutput": include_actual_output,
                "includeExpectedOutput": include_expected_output,
                "includeRetrievalContext": include_retrieval_context,
                "includeContext": include_context,
                "includeToolsCalled": include_tools_called,
                "includeExpectedTools": include_expected_tools,
            },
            path={"datasetId": dataset_id},
        )

    def get_dataset_ingestion_task(
        self, dataset_id: str, dataset_ingestion_task_id: str
    ) -> DatasetIngestionTask:
        """Get Dataset Ingestion Task

        Retrieves a single ingestion task on the dataset, with its full
        configuration.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTask,
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

    async def a_get_dataset_ingestion_task(
        self, dataset_id: str, dataset_ingestion_task_id: str
    ) -> DatasetIngestionTask:
        """Get Dataset Ingestion Task

        Retrieves a single ingestion task on the dataset, with its full
        configuration.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTask,
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

    def update_dataset_ingestion_task(
        self,
        dataset_id: str,
        dataset_ingestion_task_id: str,
        *,
        name: Optional[str] = None,
        data_model: Optional[IngestionDataModel] = None,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        sample_rate: Optional[float] = None,
        filters: Optional[FilterSet] = None,
        max_goldens: Optional[int] = None,
        input_transformer_id: Optional[str] = None,
        output_transformer_id: Optional[str] = None,
        include_input: Optional[bool] = None,
        include_actual_output: Optional[bool] = None,
        include_expected_output: Optional[bool] = None,
        include_retrieval_context: Optional[bool] = None,
        include_context: Optional[bool] = None,
        include_tools_called: Optional[bool] = None,
        include_expected_tools: Optional[bool] = None
    ) -> DatasetIngestionTask:
        """Update Dataset Ingestion Task

        Updates an ingestion task and returns it. Only the fields you send are
        changed, and at least one is required; send null to clear a nullable
        field. Toggling `enabled` schedules or unschedules the harvesting job.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
            name: A new name for the task, unique within the dataset.
            description: A note about what the task harvests. Send null to clear
                it.
            enabled: Whether the task runs. Disabling it unschedules the
                harvesting job, and goldens already created are kept. Defaults
                to false.
            sample_rate: The fraction of matching items to ingest, between 0 and
                1. Defaults to 1, all of them.
            max_goldens: The maximum number of goldens this task will ever
                create. Send null to remove the cap.
            input_transformer_id: The id of a transformer that reshapes the
                harvested input before it is stored. Send null to detach it.
            output_transformer_id: The id of a transformer that reshapes the
                harvested output before it is stored. Send null to detach it.
            include_input: Populate the golden's `input` from the harvested
                item. Defaults to true; every other include flag defaults to
                false.
            include_actual_output: Populate the golden's `actualOutput` from the
                harvested item.
            include_expected_output: Populate the golden's `expectedOutput` from
                the harvested item.
            include_retrieval_context: Populate the golden's `retrievalContext`
                from the harvested item.
            include_context: Populate the golden's `context` from the harvested
                item.
            include_tools_called: Populate the golden's `toolsCalled` from the
                harvested item.
            include_expected_tools: Populate the golden's `expectedTools` from
                the harvested item.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTask,
            request_schema=UpdateDatasetIngestionTaskRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "description": description,
                "enabled": enabled,
                "sampleRate": sample_rate,
                "filters": filters,
                "maxGoldens": max_goldens,
                "inputTransformerId": input_transformer_id,
                "outputTransformerId": output_transformer_id,
                "includeInput": include_input,
                "includeActualOutput": include_actual_output,
                "includeExpectedOutput": include_expected_output,
                "includeRetrievalContext": include_retrieval_context,
                "includeContext": include_context,
                "includeToolsCalled": include_tools_called,
                "includeExpectedTools": include_expected_tools,
            },
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

    async def a_update_dataset_ingestion_task(
        self,
        dataset_id: str,
        dataset_ingestion_task_id: str,
        *,
        name: Optional[str] = None,
        data_model: Optional[IngestionDataModel] = None,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        sample_rate: Optional[float] = None,
        filters: Optional[FilterSet] = None,
        max_goldens: Optional[int] = None,
        input_transformer_id: Optional[str] = None,
        output_transformer_id: Optional[str] = None,
        include_input: Optional[bool] = None,
        include_actual_output: Optional[bool] = None,
        include_expected_output: Optional[bool] = None,
        include_retrieval_context: Optional[bool] = None,
        include_context: Optional[bool] = None,
        include_tools_called: Optional[bool] = None,
        include_expected_tools: Optional[bool] = None
    ) -> DatasetIngestionTask:
        """Update Dataset Ingestion Task

        Updates an ingestion task and returns it. Only the fields you send are
        changed, and at least one is required; send null to clear a nullable
        field. Toggling `enabled` schedules or unschedules the harvesting job.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
            name: A new name for the task, unique within the dataset.
            description: A note about what the task harvests. Send null to clear
                it.
            enabled: Whether the task runs. Disabling it unschedules the
                harvesting job, and goldens already created are kept. Defaults
                to false.
            sample_rate: The fraction of matching items to ingest, between 0 and
                1. Defaults to 1, all of them.
            max_goldens: The maximum number of goldens this task will ever
                create. Send null to remove the cap.
            input_transformer_id: The id of a transformer that reshapes the
                harvested input before it is stored. Send null to detach it.
            output_transformer_id: The id of a transformer that reshapes the
                harvested output before it is stored. Send null to detach it.
            include_input: Populate the golden's `input` from the harvested
                item. Defaults to true; every other include flag defaults to
                false.
            include_actual_output: Populate the golden's `actualOutput` from the
                harvested item.
            include_expected_output: Populate the golden's `expectedOutput` from
                the harvested item.
            include_retrieval_context: Populate the golden's `retrievalContext`
                from the harvested item.
            include_context: Populate the golden's `context` from the harvested
                item.
            include_tools_called: Populate the golden's `toolsCalled` from the
                harvested item.
            include_expected_tools: Populate the golden's `expectedTools` from
                the harvested item.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTask,
            request_schema=UpdateDatasetIngestionTaskRequest,
            body={
                "name": name,
                "dataModel": data_model,
                "description": description,
                "enabled": enabled,
                "sampleRate": sample_rate,
                "filters": filters,
                "maxGoldens": max_goldens,
                "inputTransformerId": input_transformer_id,
                "outputTransformerId": output_transformer_id,
                "includeInput": include_input,
                "includeActualOutput": include_actual_output,
                "includeExpectedOutput": include_expected_output,
                "includeRetrievalContext": include_retrieval_context,
                "includeContext": include_context,
                "includeToolsCalled": include_tools_called,
                "includeExpectedTools": include_expected_tools,
            },
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

    def delete_dataset_ingestion_task(
        self, dataset_id: str, dataset_ingestion_task_id: str
    ) -> DatasetIngestionTaskRef:
        """Delete Dataset Ingestion Task

        Permanently deletes an ingestion task and unschedules its harvesting
        job. Goldens it already created stay in the dataset. This action cannot
        be undone.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTaskRef,
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

    async def a_delete_dataset_ingestion_task(
        self, dataset_id: str, dataset_ingestion_task_id: str
    ) -> DatasetIngestionTaskRef:
        """Delete Dataset Ingestion Task

        Permanently deletes an ingestion task and unschedules its harvesting
        job. Goldens it already created stay in the dataset. This action cannot
        be undone.

        Args:
            dataset_id: The unique id of the dataset.
            dataset_ingestion_task_id: The unique id of the ingestion task.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.DATASET_DATASET_INGESTION_TASK_ENDPOINT,
            response_schema=DatasetIngestionTaskRef,
            path={
                "datasetId": dataset_id,
                "datasetIngestionTaskId": dataset_ingestion_task_id,
            },
        )

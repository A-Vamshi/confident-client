# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: datasets).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.datasets.types import (
    CreateGoldenRequest,
    Golden,
    GoldenRef,
    GoldenRequest,
)
from confidentai.endpoints import Endpoints


class GoldensOperations:
    _api: Api

    def create_golden(
        self,
        dataset_id: str,
        golden: GoldenRequest,
        *,
        version: Optional[str] = None
    ) -> GoldenRef:
        """Create Golden

        Adds a single golden to the dataset and returns its id. The golden's
        kind must match the dataset's `multiTurn`. Pass `version` to add it to a
        specific dataset version; omitting it targets the latest version.

        Args:
            dataset_id: The unique id of the dataset.
            version: The dataset version to add the golden to. Omitting it
                targets the latest version, or the unversioned goldens when the
                dataset has no versions.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.DATASET_GOLDENS_ENDPOINT,
            response_schema=GoldenRef,
            request_schema=CreateGoldenRequest,
            body={"golden": golden, "version": version},
            path={"datasetId": dataset_id},
        )

    async def a_create_golden(
        self,
        dataset_id: str,
        golden: GoldenRequest,
        *,
        version: Optional[str] = None
    ) -> GoldenRef:
        """Create Golden

        Adds a single golden to the dataset and returns its id. The golden's
        kind must match the dataset's `multiTurn`. Pass `version` to add it to a
        specific dataset version; omitting it targets the latest version.

        Args:
            dataset_id: The unique id of the dataset.
            version: The dataset version to add the golden to. Omitting it
                targets the latest version, or the unversioned goldens when the
                dataset has no versions.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.DATASET_GOLDENS_ENDPOINT,
            response_schema=GoldenRef,
            request_schema=CreateGoldenRequest,
            body={"golden": golden, "version": version},
            path={"datasetId": dataset_id},
        )

    def get_golden(self, dataset_id: str, golden_id: str) -> Golden:
        """Get Golden

        Retrieves a single golden by id. It is single-turn or multi-turn
        according to the dataset's `multiTurn`.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=Golden,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

    async def a_get_golden(self, dataset_id: str, golden_id: str) -> Golden:
        """Get Golden

        Retrieves a single golden by id. It is single-turn or multi-turn
        according to the dataset's `multiTurn`.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=Golden,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

    def update_golden(
        self, dataset_id: str, golden_id: str, golden: GoldenRequest
    ) -> GoldenRef:
        """Update Golden

        Replaces the fields of a single golden with the values you send and
        returns its id. `sourceFile`, `sourceFiles`, `tags` and
        `customColumnKeyValues` are left unchanged unless you include them. The
        golden's kind must match the dataset's `multiTurn`.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
            golden: One golden to write: single-turn when it carries `input`,
                multi-turn when it carries `scenario`. A golden cannot be both,
                and its kind must match the dataset's `multiTurn`. Pass a
                SingleTurnGoldenRequest or a MultiTurnGoldenRequest, from
                confidentai.datasets.types.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=GoldenRef,
            request_schema=GoldenRequest,
            body=golden,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

    async def a_update_golden(
        self, dataset_id: str, golden_id: str, golden: GoldenRequest
    ) -> GoldenRef:
        """Update Golden

        Replaces the fields of a single golden with the values you send and
        returns its id. `sourceFile`, `sourceFiles`, `tags` and
        `customColumnKeyValues` are left unchanged unless you include them. The
        golden's kind must match the dataset's `multiTurn`.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
            golden: One golden to write: single-turn when it carries `input`,
                multi-turn when it carries `scenario`. A golden cannot be both,
                and its kind must match the dataset's `multiTurn`. Pass a
                SingleTurnGoldenRequest or a MultiTurnGoldenRequest, from
                confidentai.datasets.types.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=GoldenRef,
            request_schema=GoldenRequest,
            body=golden,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

    def delete_golden(self, dataset_id: str, golden_id: str) -> GoldenRef:
        """Delete Golden

        Permanently deletes a single golden. The rest of the dataset is
        unchanged, and this action cannot be undone.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=GoldenRef,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

    async def a_delete_golden(
        self, dataset_id: str, golden_id: str
    ) -> GoldenRef:
        """Delete Golden

        Permanently deletes a single golden. The rest of the dataset is
        unchanged, and this action cannot be undone.

        Args:
            dataset_id: The unique id of the dataset.
            golden_id: The unique id of the golden, returned when the dataset is
                pulled.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.DATASET_GOLDEN_ENDPOINT,
            response_schema=GoldenRef,
            path={"datasetId": dataset_id, "goldenId": golden_id},
        )

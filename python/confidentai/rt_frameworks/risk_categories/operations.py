# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: rt-frameworks).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Dict, List, Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.rt_frameworks.types import (
    CreateRiskCategoryRequest,
    RiskCategory,
    RiskCategoryList,
    RiskCategoryRef,
    UpdateRiskCategoryRequest,
)


class RiskCategoriesOperations:
    _api: Api

    def list_risk_categories(
        self,
        rt_framework_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> RiskCategoryList:
        """List Risk Categories

        Lists the risk categories of a red teaming framework one page at a time,
        ordered by name. Each is returned with its selections counted; retrieve
        one by id to see which vulnerability types and attack methods it holds.

        Args:
            rt_framework_id: The id of the red teaming framework.
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORIES_ENDPOINT,
            response_schema=RiskCategoryList,
            path={"rtFrameworkId": rt_framework_id},
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_risk_categories(
        self,
        rt_framework_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> RiskCategoryList:
        """List Risk Categories

        Lists the risk categories of a red teaming framework one page at a time,
        ordered by name. Each is returned with its selections counted; retrieve
        one by id to see which vulnerability types and attack methods it holds.

        Args:
            rt_framework_id: The id of the red teaming framework.
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORIES_ENDPOINT,
            response_schema=RiskCategoryList,
            path={"rtFrameworkId": rt_framework_id},
            query={"page": page, "pageSize": page_size},
        )

    def create_risk_category(
        self,
        rt_framework_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        vulnerability_type_ids: Optional[List[str]] = None,
        attack_method_ids: Optional[List[str]] = None,
        vulnerability_id_to_priority_level: Optional[Dict[str, int]] = None
    ) -> RiskCategoryRef:
        """Create Risk Category

        Adds a risk category to a red teaming framework and returns its id. Send
        `vulnerabilityTypeIds` and `attackMethodIds` to fill it in the same
        call: a category with neither probes for nothing when the framework
        runs.

        Args:
            rt_framework_id: The id of the red teaming framework.
            name: The name of the risk category, unique within the framework.
            description: What this risk category covers. Send null to clear it.
            vulnerability_type_ids: The ids of the vulnerability types to probe
                for. The list replaces the category's current selection.
            attack_method_ids: The ids of the attack methods to probe with. The
                list replaces the category's current selection.
            vulnerability_id_to_priority_level: How much of the assessment each
                vulnerability gets, keyed by vulnerability id, from 0 to 3. Send
                null to clear every weight.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORIES_ENDPOINT,
            response_schema=RiskCategoryRef,
            request_schema=CreateRiskCategoryRequest,
            body={
                "name": name,
                "description": description,
                "vulnerabilityTypeIds": vulnerability_type_ids,
                "attackMethodIds": attack_method_ids,
                "vulnerabilityIdToPriorityLevel": vulnerability_id_to_priority_level,
            },
            path={"rtFrameworkId": rt_framework_id},
        )

    async def a_create_risk_category(
        self,
        rt_framework_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        vulnerability_type_ids: Optional[List[str]] = None,
        attack_method_ids: Optional[List[str]] = None,
        vulnerability_id_to_priority_level: Optional[Dict[str, int]] = None
    ) -> RiskCategoryRef:
        """Create Risk Category

        Adds a risk category to a red teaming framework and returns its id. Send
        `vulnerabilityTypeIds` and `attackMethodIds` to fill it in the same
        call: a category with neither probes for nothing when the framework
        runs.

        Args:
            rt_framework_id: The id of the red teaming framework.
            name: The name of the risk category, unique within the framework.
            description: What this risk category covers. Send null to clear it.
            vulnerability_type_ids: The ids of the vulnerability types to probe
                for. The list replaces the category's current selection.
            attack_method_ids: The ids of the attack methods to probe with. The
                list replaces the category's current selection.
            vulnerability_id_to_priority_level: How much of the assessment each
                vulnerability gets, keyed by vulnerability id, from 0 to 3. Send
                null to clear every weight.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORIES_ENDPOINT,
            response_schema=RiskCategoryRef,
            request_schema=CreateRiskCategoryRequest,
            body={
                "name": name,
                "description": description,
                "vulnerabilityTypeIds": vulnerability_type_ids,
                "attackMethodIds": attack_method_ids,
                "vulnerabilityIdToPriorityLevel": vulnerability_id_to_priority_level,
            },
            path={"rtFrameworkId": rt_framework_id},
        )

    def get_risk_category(
        self, rt_framework_id: str, risk_category_id: str
    ) -> RiskCategory:
        """Get Risk Category

        Retrieves a risk category by id, with the vulnerability types it probes
        for, the attack methods it probes with, and how much of an assessment
        each vulnerability gets.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategory,
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

    async def a_get_risk_category(
        self, rt_framework_id: str, risk_category_id: str
    ) -> RiskCategory:
        """Get Risk Category

        Retrieves a risk category by id, with the vulnerability types it probes
        for, the attack methods it probes with, and how much of an assessment
        each vulnerability gets.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategory,
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

    def update_risk_category(
        self,
        rt_framework_id: str,
        risk_category_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        vulnerability_type_ids: Optional[List[str]] = None,
        attack_method_ids: Optional[List[str]] = None,
        vulnerability_id_to_priority_level: Optional[Dict[str, int]] = None
    ) -> RiskCategory:
        """Update Risk Category

        Updates a risk category and returns it. Each list you send replaces the
        stored selection rather than adding to it, so send the complete set of
        vulnerability type ids or attack method ids you want the category to
        hold.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
            name: The name of the risk category, unique within the framework.
            description: What this risk category covers. Send null to clear it.
            vulnerability_type_ids: The ids of the vulnerability types to probe
                for. The list replaces the category's current selection.
            attack_method_ids: The ids of the attack methods to probe with. The
                list replaces the category's current selection.
            vulnerability_id_to_priority_level: How much of the assessment each
                vulnerability gets, keyed by vulnerability id, from 0 to 3. Send
                null to clear every weight.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategory,
            request_schema=UpdateRiskCategoryRequest,
            body={
                "name": name,
                "description": description,
                "vulnerabilityTypeIds": vulnerability_type_ids,
                "attackMethodIds": attack_method_ids,
                "vulnerabilityIdToPriorityLevel": vulnerability_id_to_priority_level,
            },
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

    async def a_update_risk_category(
        self,
        rt_framework_id: str,
        risk_category_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        vulnerability_type_ids: Optional[List[str]] = None,
        attack_method_ids: Optional[List[str]] = None,
        vulnerability_id_to_priority_level: Optional[Dict[str, int]] = None
    ) -> RiskCategory:
        """Update Risk Category

        Updates a risk category and returns it. Each list you send replaces the
        stored selection rather than adding to it, so send the complete set of
        vulnerability type ids or attack method ids you want the category to
        hold.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
            name: The name of the risk category, unique within the framework.
            description: What this risk category covers. Send null to clear it.
            vulnerability_type_ids: The ids of the vulnerability types to probe
                for. The list replaces the category's current selection.
            attack_method_ids: The ids of the attack methods to probe with. The
                list replaces the category's current selection.
            vulnerability_id_to_priority_level: How much of the assessment each
                vulnerability gets, keyed by vulnerability id, from 0 to 3. Send
                null to clear every weight.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategory,
            request_schema=UpdateRiskCategoryRequest,
            body={
                "name": name,
                "description": description,
                "vulnerabilityTypeIds": vulnerability_type_ids,
                "attackMethodIds": attack_method_ids,
                "vulnerabilityIdToPriorityLevel": vulnerability_id_to_priority_level,
            },
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

    def delete_risk_category(
        self, rt_framework_id: str, risk_category_id: str
    ) -> RiskCategoryRef:
        """Delete Risk Category

        Permanently deletes a risk category from its framework, along with the
        selections and weights it held. The vulnerabilities and attack methods
        themselves are untouched.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategoryRef,
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

    async def a_delete_risk_category(
        self, rt_framework_id: str, risk_category_id: str
    ) -> RiskCategoryRef:
        """Delete Risk Category

        Permanently deletes a risk category from its framework, along with the
        selections and weights it held. The vulnerabilities and attack methods
        themselves are untouched.

        Args:
            rt_framework_id: The id of the red teaming framework the category
                belongs to.
            risk_category_id: The id of the risk category.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.RT_FRAMEWORK_RISK_CATEGORY_ENDPOINT,
            response_schema=RiskCategoryRef,
            path={
                "rtFrameworkId": rt_framework_id,
                "riskCategoryId": risk_category_id,
            },
        )

# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: transformers).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Any, Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.transformers.types import (
    CreateTransformerRequest,
    TestTransformerCodeRequest,
    Transformer,
    TransformerCodeDefinition,
    TransformerCodeRunResult,
    TransformerList,
    TransformerRef,
    UpdateTransformerRequest,
)


class TransformersClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list_transformers(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> TransformerList:
        """List Transformers

        Lists the transformers in your Confident AI project one page at a time,
        ordered by name. Use the returned ids for an AI connection's or a metric
        collection's transformer fields; retrieve one by id to read its code.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.TRANSFORMERS_ENDPOINT,
            response_schema=TransformerList,
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_transformers(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> TransformerList:
        """List Transformers

        Lists the transformers in your Confident AI project one page at a time,
        ordered by name. Use the returned ids for an AI connection's or a metric
        collection's transformer fields; retrieve one by id to read its code.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.TRANSFORMERS_ENDPOINT,
            response_schema=TransformerList,
            query={"page": page, "pageSize": page_size},
        )

    def create_transformer(
        self,
        name: str,
        code_definition: TransformerCodeDefinition,
        *,
        description: Optional[str] = None
    ) -> TransformerRef:
        """Create Transformer

        Creates a transformer in your Confident AI project and returns its id.
        Test the code against a sample before attaching the transformer to
        anything.

        Args:
            name: The name of the transformer, unique within the project.
            description: What the transformer extracts. Send null to leave it
                unset.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.TRANSFORMERS_ENDPOINT,
            response_schema=TransformerRef,
            request_schema=CreateTransformerRequest,
            body={
                "name": name,
                "codeDefinition": code_definition,
                "description": description,
            },
        )

    async def a_create_transformer(
        self,
        name: str,
        code_definition: TransformerCodeDefinition,
        *,
        description: Optional[str] = None
    ) -> TransformerRef:
        """Create Transformer

        Creates a transformer in your Confident AI project and returns its id.
        Test the code against a sample before attaching the transformer to
        anything.

        Args:
            name: The name of the transformer, unique within the project.
            description: What the transformer extracts. Send null to leave it
                unset.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.TRANSFORMERS_ENDPOINT,
            response_schema=TransformerRef,
            request_schema=CreateTransformerRequest,
            body={
                "name": name,
                "codeDefinition": code_definition,
                "description": description,
            },
        )

    def get_transformer(self, transformer_id: str) -> Transformer:
        """Get Transformer

        Retrieves a transformer by id, including the code it runs. A transformer
        saved without code returns `codeDefinition` as null.

        Args:
            transformer_id: The id of the transformer.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=Transformer,
            path={"transformerId": transformer_id},
        )

    async def a_get_transformer(self, transformer_id: str) -> Transformer:
        """Get Transformer

        Retrieves a transformer by id, including the code it runs. A transformer
        saved without code returns `codeDefinition` as null.

        Args:
            transformer_id: The id of the transformer.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=Transformer,
            path={"transformerId": transformer_id},
        )

    def update_transformer(
        self,
        transformer_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        code_definition: Optional[TransformerCodeDefinition] = None
    ) -> Transformer:
        """Update Transformer

        Renames a transformer, changes its description, or replaces its code,
        and returns it. Sending `codeDefinition` overwrites the stored code,
        which every AI connection and metric collection already using this
        transformer picks up on its next run.

        Args:
            transformer_id: The id of the transformer.
            name: The name of the transformer, unique within the project.
            description: What the transformer extracts. Send null to clear it.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=Transformer,
            request_schema=UpdateTransformerRequest,
            body={
                "name": name,
                "description": description,
                "codeDefinition": code_definition,
            },
            path={"transformerId": transformer_id},
        )

    async def a_update_transformer(
        self,
        transformer_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        code_definition: Optional[TransformerCodeDefinition] = None
    ) -> Transformer:
        """Update Transformer

        Renames a transformer, changes its description, or replaces its code,
        and returns it. Sending `codeDefinition` overwrites the stored code,
        which every AI connection and metric collection already using this
        transformer picks up on its next run.

        Args:
            transformer_id: The id of the transformer.
            name: The name of the transformer, unique within the project.
            description: What the transformer extracts. Send null to clear it.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=Transformer,
            request_schema=UpdateTransformerRequest,
            body={
                "name": name,
                "description": description,
                "codeDefinition": code_definition,
            },
            path={"transformerId": transformer_id},
        )

    def delete_transformer(self, transformer_id: str) -> TransformerRef:
        """Delete Transformer

        Permanently deletes a transformer and the code stored with it. Anything
        still pointing at it stops transforming.

        Args:
            transformer_id: The id of the transformer.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=TransformerRef,
            path={"transformerId": transformer_id},
        )

    async def a_delete_transformer(self, transformer_id: str) -> TransformerRef:
        """Delete Transformer

        Permanently deletes a transformer and the code stored with it. Anything
        still pointing at it stops transforming.

        Args:
            transformer_id: The id of the transformer.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.TRANSFORMER_ENDPOINT,
            response_schema=TransformerRef,
            path={"transformerId": transformer_id},
        )

    def test_transformer_code(
        self, transformer_id: str, *, input_data: Optional[Any] = None
    ) -> TransformerCodeRunResult:
        """Test Transformer Code

        Runs the transformer's stored code against a sample value and returns
        what it produced. Code that raises or times out is a completed test, so
        it comes back with a 200 and `success: false` carrying `error` and
        `reason` — branch on `success` rather than on the status. A 404 means
        the transformer does not exist in this project, and a 400 means it has
        no code saved to run.

        Args:
            transformer_id: The id of the transformer.
            input_data: The value to pass to the transformer's `transform`
                function. Any JSON value is accepted; send the shape the
                transformer expects to see in production.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.TRANSFORMER_TEST_CODE_ENDPOINT,
            response_schema=TransformerCodeRunResult,
            request_schema=TestTransformerCodeRequest,
            body={"inputData": input_data},
            path={"transformerId": transformer_id},
        )

    async def a_test_transformer_code(
        self, transformer_id: str, *, input_data: Optional[Any] = None
    ) -> TransformerCodeRunResult:
        """Test Transformer Code

        Runs the transformer's stored code against a sample value and returns
        what it produced. Code that raises or times out is a completed test, so
        it comes back with a 200 and `success: false` carrying `error` and
        `reason` — branch on `success` rather than on the status. A 404 means
        the transformer does not exist in this project, and a 400 means it has
        no code saved to run.

        Args:
            transformer_id: The id of the transformer.
            input_data: The value to pass to the transformer's `transform`
                function. Any JSON value is accepted; send the shape the
                transformer expects to see in production.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.TRANSFORMER_TEST_CODE_ENDPOINT,
            response_schema=TransformerCodeRunResult,
            request_schema=TestTransformerCodeRequest,
            body={"inputData": input_data},
            path={"transformerId": transformer_id},
        )

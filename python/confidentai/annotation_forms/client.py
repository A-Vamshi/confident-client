# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: annotation-forms).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List, Optional

from confidentai.annotation_forms.types import (
    AnnotationForm,
    AnnotationFormFieldConfig,
    AnnotationFormList,
    AnnotationFormRef,
    CreateAnnotationFormRequest,
    UpdateAnnotationFormRequest,
)
from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints


class AnnotationFormsClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list_annotation_forms(self) -> AnnotationFormList:
        """List Annotation Forms

        Lists the annotation forms in your Confident AI project, oldest first.
        Each form is returned as a summary with its field and queue counts;
        retrieve a form by id for its questions.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_FORMS_ENDPOINT,
            response_schema=AnnotationFormList,
        )

    async def a_list_annotation_forms(self) -> AnnotationFormList:
        """List Annotation Forms

        Lists the annotation forms in your Confident AI project, oldest first.
        Each form is returned as a summary with its field and queue counts;
        retrieve a form by id for its questions.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_FORMS_ENDPOINT,
            response_schema=AnnotationFormList,
        )

    def create_annotation_form(
        self,
        name: str,
        *,
        fields: Optional[List[AnnotationFormFieldConfig]] = None
    ) -> AnnotationFormRef:
        """Create Annotation Form

        Creates an annotation form in your Confident AI project and returns its
        id. Attach the form to an annotation queue to have its questions asked
        of every item in that queue.

        Args:
            name: The name of the form, shown wherever a queue offers it.
            fields: The questions to put on the form, in the order annotators
                see them. A form created without fields collects nothing until
                you add some.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_FORMS_ENDPOINT,
            response_schema=AnnotationFormRef,
            request_schema=CreateAnnotationFormRequest,
            body={"name": name, "fields": fields},
        )

    async def a_create_annotation_form(
        self,
        name: str,
        *,
        fields: Optional[List[AnnotationFormFieldConfig]] = None
    ) -> AnnotationFormRef:
        """Create Annotation Form

        Creates an annotation form in your Confident AI project and returns its
        id. Attach the form to an annotation queue to have its questions asked
        of every item in that queue.

        Args:
            name: The name of the form, shown wherever a queue offers it.
            fields: The questions to put on the form, in the order annotators
                see them. A form created without fields collects nothing until
                you add some.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_FORMS_ENDPOINT,
            response_schema=AnnotationFormRef,
            request_schema=CreateAnnotationFormRequest,
            body={"name": name, "fields": fields},
        )

    def get_annotation_form(self, annotation_form_id: str) -> AnnotationForm:
        """Get Annotation Form

        Retrieves an annotation form by id from your Confident AI project, with
        its questions in the order annotators see them.

        Args:
            annotation_form_id: The id of the annotation form.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationForm,
            path={"annotationFormId": annotation_form_id},
        )

    async def a_get_annotation_form(
        self, annotation_form_id: str
    ) -> AnnotationForm:
        """Get Annotation Form

        Retrieves an annotation form by id from your Confident AI project, with
        its questions in the order annotators see them.

        Args:
            annotation_form_id: The id of the annotation form.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationForm,
            path={"annotationFormId": annotation_form_id},
        )

    def update_annotation_form(
        self,
        annotation_form_id: str,
        *,
        name: Optional[str] = None,
        fields: Optional[List[AnnotationFormFieldConfig]] = None
    ) -> AnnotationForm:
        """Update Annotation Form

        Updates an annotation form and returns it. Sending `fields` replaces the
        stored questions: a field sent with its `id` keeps the answers already
        recorded against it, and one left out is removed along with them.

        Args:
            annotation_form_id: The id of the annotation form.
            name: The name of the form, shown wherever a queue offers it.
            fields: The complete list of questions the form should carry. It
                replaces the stored fields: a field you send with its `id` keeps
                its recorded answers, and one you leave out is removed along
                with them.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationForm,
            request_schema=UpdateAnnotationFormRequest,
            body={"name": name, "fields": fields},
            path={"annotationFormId": annotation_form_id},
        )

    async def a_update_annotation_form(
        self,
        annotation_form_id: str,
        *,
        name: Optional[str] = None,
        fields: Optional[List[AnnotationFormFieldConfig]] = None
    ) -> AnnotationForm:
        """Update Annotation Form

        Updates an annotation form and returns it. Sending `fields` replaces the
        stored questions: a field sent with its `id` keeps the answers already
        recorded against it, and one left out is removed along with them.

        Args:
            annotation_form_id: The id of the annotation form.
            name: The name of the form, shown wherever a queue offers it.
            fields: The complete list of questions the form should carry. It
                replaces the stored fields: a field you send with its `id` keeps
                its recorded answers, and one you leave out is removed along
                with them.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationForm,
            request_schema=UpdateAnnotationFormRequest,
            body={"name": name, "fields": fields},
            path={"annotationFormId": annotation_form_id},
        )

    def delete_annotation_form(
        self, annotation_form_id: str
    ) -> AnnotationFormRef:
        """Delete Annotation Form

        Permanently deletes an annotation form and the answers recorded against
        its questions. Queues using the form keep working, with no form
        attached.

        Args:
            annotation_form_id: The id of the annotation form.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationFormRef,
            path={"annotationFormId": annotation_form_id},
        )

    async def a_delete_annotation_form(
        self, annotation_form_id: str
    ) -> AnnotationFormRef:
        """Delete Annotation Form

        Permanently deletes an annotation form and the answers recorded against
        its questions. Queues using the form keep working, with no form
        attached.

        Args:
            annotation_form_id: The id of the annotation form.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.ANNOTATION_FORM_ENDPOINT,
            response_schema=AnnotationFormRef,
            path={"annotationFormId": annotation_form_id},
        )

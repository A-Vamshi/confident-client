# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: prompts).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.prompts.types import (
    CreatePromptVersionRequest,
    CreatePromptVersionResult,
    Prompt,
    PromptVersionList,
)


class VersionsOperations:
    _api: Api

    def get_versions(self, prompt_id: str) -> PromptVersionList:
        """List Prompt Versions

        Lists every version released for the prompt, oldest first.

        Args:
            prompt_id: The unique id of the prompt.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.PROMPT_VERSIONS_ENDPOINT,
            response_schema=PromptVersionList,
            path={"promptId": prompt_id},
        )

    async def a_get_versions(self, prompt_id: str) -> PromptVersionList:
        """List Prompt Versions

        Lists every version released for the prompt, oldest first.

        Args:
            prompt_id: The unique id of the prompt.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.PROMPT_VERSIONS_ENDPOINT,
            response_schema=PromptVersionList,
            path={"promptId": prompt_id},
        )

    def create_version(
        self, prompt_id: str, *, hash: Optional[str] = None
    ) -> CreatePromptVersionResult:
        """Create Prompt Version

        Releases a commit as a new version of the prompt. Versions the commit
        with the given `hash`, or the head commit of `main` when no hash is
        given.

        Args:
            prompt_id: The unique id of the prompt.
            hash: The hash of the commit to release as a new version. Only
                commits newer than the last versioned commit can be released.
                When omitted, the head commit of `main` is versioned.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.PROMPT_VERSIONS_ENDPOINT,
            response_schema=CreatePromptVersionResult,
            request_schema=CreatePromptVersionRequest,
            body={"hash": hash},
            path={"promptId": prompt_id},
        )

    async def a_create_version(
        self, prompt_id: str, *, hash: Optional[str] = None
    ) -> CreatePromptVersionResult:
        """Create Prompt Version

        Releases a commit as a new version of the prompt. Versions the commit
        with the given `hash`, or the head commit of `main` when no hash is
        given.

        Args:
            prompt_id: The unique id of the prompt.
            hash: The hash of the commit to release as a new version. Only
                commits newer than the last versioned commit can be released.
                When omitted, the head commit of `main` is versioned.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.PROMPT_VERSIONS_ENDPOINT,
            response_schema=CreatePromptVersionResult,
            request_schema=CreatePromptVersionRequest,
            body={"hash": hash},
            path={"promptId": prompt_id},
        )

    def get_by_version(self, prompt_id: str, version: str) -> Prompt:
        """Pull Prompt by Version

        Retrieves the prompt commit released as `version`.

        Args:
            prompt_id: The unique id of the prompt.
            version: The version number of the prompt to pull.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.PROMPT_VERSION_ENDPOINT,
            response_schema=Prompt,
            path={"promptId": prompt_id, "version": version},
        )

    async def a_get_by_version(self, prompt_id: str, version: str) -> Prompt:
        """Pull Prompt by Version

        Retrieves the prompt commit released as `version`.

        Args:
            prompt_id: The unique id of the prompt.
            version: The version number of the prompt to pull.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.PROMPT_VERSION_ENDPOINT,
            response_schema=Prompt,
            path={"promptId": prompt_id, "version": version},
        )

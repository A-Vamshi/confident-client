# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: prompts).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List, Optional

from confidentai.api import Api
from confidentai.prompts.client import PromptsClient
from confidentai.utils.helpers import interpolate_prompt
from confidentai.common.types import PromptType
from confidentai.prompts.types import (
    CreatePromptVersionResult,
    ModelSettings,
    OutputSchema,
    Prompt as PromptPayload,
    PromptBranch,
    PromptBranchList,
    PromptBranchRef,
    PromptCommitList,
    PromptInterpolationType,
    PromptMessage,
    PromptOutputType,
    PromptVersionList,
    PushMessagesPrompt,
    PushPromptRequest,
    PushPromptResult,
    PushTextPrompt,
    Tool,
)


class Prompt:
    """One prompt of your project, held by its prompt_id.

    Its methods are PromptsClient's, minus the
    `promptId` that this handle supplies.
    """

    def __init__(
        self,
        api: Api,
        *,
        prompt_id: Optional[str] = None,
        alias: Optional[str] = None,
    ) -> None:
        self._client = PromptsClient(api)
        self.prompt_id: Optional[str] = prompt_id
        self.text: Optional[str] = None
        self.alias: Optional[str] = alias
        self.id: Optional[str] = None
        self.hash: Optional[str] = None
        self.version: Optional[str] = None
        self.label: Optional[str] = None
        self.type: Optional[PromptType] = None
        self.interpolation_type: Optional[PromptInterpolationType] = None
        self.model_settings: Optional[ModelSettings] = None
        self.output_type: Optional[PromptOutputType] = None
        self.output_schema: Optional[OutputSchema] = None
        self.tools: Optional[List[Tool]] = None
        self.messages: Optional[List[PromptMessage]] = None

    def __repr__(self) -> str:
        return f"Prompt(prompt_id={self.prompt_id!r}, alias={self.alias!r})"

    def list_versions(self) -> PromptVersionList:
        """List Prompt Versions

        Lists every version released for the prompt, oldest first.
        """
        return self._client.get_versions(self._prompt_id())

    async def a_list_versions(self) -> PromptVersionList:
        """List Prompt Versions

        Lists every version released for the prompt, oldest first.
        """
        return await self._client.a_get_versions(self._prompt_id())

    def create_version(
        self, *, hash: Optional[str] = None
    ) -> CreatePromptVersionResult:
        """Create Prompt Version

        Releases a commit as a new version of the prompt. Versions the commit
        with the given `hash`, or the head commit of `main` when no hash is
        given.

        Args:
            hash: The hash of the commit to release as a new version. Only
                commits newer than the last versioned commit can be released.
                When omitted, the head commit of `main` is versioned.
        """
        return self._client.create_version(self._prompt_id(), hash=hash)

    async def a_create_version(
        self, *, hash: Optional[str] = None
    ) -> CreatePromptVersionResult:
        """Create Prompt Version

        Releases a commit as a new version of the prompt. Versions the commit
        with the given `hash`, or the head commit of `main` when no hash is
        given.

        Args:
            hash: The hash of the commit to release as a new version. Only
                commits newer than the last versioned commit can be released.
                When omitted, the head commit of `main` is versioned.
        """
        return await self._client.a_create_version(self._prompt_id(), hash=hash)

    def list_commits(self, *, branch: Optional[str] = None) -> PromptCommitList:
        """List Prompt Commits

        Lists the commits of the prompt, newest first. Pass `branch` to list
        only the commits reachable from that branch.

        Args:
            branch: The name of the branch to read from. Defaults to `main` when
                omitted.
        """
        return self._client.get_commits(self._prompt_id(), branch=branch)

    async def a_list_commits(
        self, *, branch: Optional[str] = None
    ) -> PromptCommitList:
        """List Prompt Commits

        Lists the commits of the prompt, newest first. Pass `branch` to list
        only the commits reachable from that branch.

        Args:
            branch: The name of the branch to read from. Defaults to `main` when
                omitted.
        """
        return await self._client.a_get_commits(
            self._prompt_id(), branch=branch
        )

    def list_branches(self) -> PromptBranchList:
        """List Prompt Branches

        Lists the branches of the prompt.
        """
        return self._client.get_branches(self._prompt_id())

    async def a_list_branches(self) -> PromptBranchList:
        """List Prompt Branches

        Lists the branches of the prompt.
        """
        return await self._client.a_get_branches(self._prompt_id())

    def create_branch(self, name: str) -> PromptBranch:
        """Create Prompt Branch

        Creates a branch of the prompt diverging from the head commit of `main`.

        Args:
            name: The name of the branch to create. It diverges from the head
                commit of `main`.
        """
        return self._client.create_branch(self._prompt_id(), name)

    async def a_create_branch(self, name: str) -> PromptBranch:
        """Create Prompt Branch

        Creates a branch of the prompt diverging from the head commit of `main`.

        Args:
            name: The name of the branch to create. It diverges from the head
                commit of `main`.
        """
        return await self._client.a_create_branch(self._prompt_id(), name)

    def update_branch(self, branch_id: str, name: str) -> PromptBranchRef:
        """Update Prompt Branch

        Renames a branch of the prompt. The `main` branch cannot be renamed.

        Args:
            branch_id: The unique id of the branch.
            name: The new name of the branch. The `main` branch cannot be
                renamed.
        """
        return self._client.update_branch(self._prompt_id(), branch_id, name)

    async def a_update_branch(
        self, branch_id: str, name: str
    ) -> PromptBranchRef:
        """Update Prompt Branch

        Renames a branch of the prompt. The `main` branch cannot be renamed.

        Args:
            branch_id: The unique id of the branch.
            name: The new name of the branch. The `main` branch cannot be
                renamed.
        """
        return await self._client.a_update_branch(
            self._prompt_id(), branch_id, name
        )

    def delete_branch(self, branch_id: str) -> PromptBranchRef:
        """Delete Prompt Branch

        Deletes a branch of the prompt. The `main` branch, branches with open
        pull requests, and branches referenced by AI connections cannot be
        deleted.

        Args:
            branch_id: The unique id of the branch.
        """
        return self._client.delete_branch(self._prompt_id(), branch_id)

    async def a_delete_branch(self, branch_id: str) -> PromptBranchRef:
        """Delete Prompt Branch

        Deletes a branch of the prompt. The `main` branch, branches with open
        pull requests, and branches referenced by AI connections cannot be
        deleted.

        Args:
            branch_id: The unique id of the branch.
        """
        return await self._client.a_delete_branch(self._prompt_id(), branch_id)

    def pull(
        self,
        *,
        commit: Optional[str] = None,
        version: Optional[str] = None,
        label: Optional[str] = None,
        branch: Optional[str] = None,
    ) -> "Prompt":
        """Pull

        A single commit of a prompt, as pulled by version, commit hash, or
        label. It carries `text` when the prompt is a text prompt and `messages`
        when it is a messages prompt, never both. Pass at most one of commit,
        version, label; the handle is filled in from the result.

        Args:
            commit: The hash of the commit to pull. Defaults to `latest`.
            version: The version number of the prompt to pull.
            label: The label of the version to pull.
            branch: The name of the branch to read from. Defaults to `main` when
                omitted. Only valid with `commit`.
        """
        given = [
            name
            for name, value in (
                ("commit", commit),
                ("version", version),
                ("label", label),
            )
            if value is not None
        ]
        if len(given) > 1:
            raise ValueError(
                f"Provide at most one of commit, version, label; got {', '.join(given)}."
            )
        if branch is not None and given and given != ["commit"]:
            raise ValueError(
                f"`branch` narrows a commit lookup, so it cannot be combined with {given[0]}."
            )
        prompt_id = self._prompt_id()
        if version is not None:
            payload = self._client.get_by_version(prompt_id, version)
        elif label is not None:
            payload = self._client.get_by_label(prompt_id, label)
        else:
            payload = self._client.get_by_commit(
                prompt_id, commit or "latest", branch=branch
            )
        self._load(payload)
        return self

    async def a_pull(
        self,
        *,
        commit: Optional[str] = None,
        version: Optional[str] = None,
        label: Optional[str] = None,
        branch: Optional[str] = None,
    ) -> "Prompt":
        """Pull

        A single commit of a prompt, as pulled by version, commit hash, or
        label. It carries `text` when the prompt is a text prompt and `messages`
        when it is a messages prompt, never both. Pass at most one of commit,
        version, label; the handle is filled in from the result.

        Args:
            commit: The hash of the commit to pull. Defaults to `latest`.
            version: The version number of the prompt to pull.
            label: The label of the version to pull.
            branch: The name of the branch to read from. Defaults to `main` when
                omitted. Only valid with `commit`.
        """
        given = [
            name
            for name, value in (
                ("commit", commit),
                ("version", version),
                ("label", label),
            )
            if value is not None
        ]
        if len(given) > 1:
            raise ValueError(
                f"Provide at most one of commit, version, label; got {', '.join(given)}."
            )
        if branch is not None and given and given != ["commit"]:
            raise ValueError(
                f"`branch` narrows a commit lookup, so it cannot be combined with {given[0]}."
            )
        prompt_id = self._prompt_id()
        if version is not None:
            payload = await self._client.a_get_by_version(prompt_id, version)
        elif label is not None:
            payload = await self._client.a_get_by_label(prompt_id, label)
        else:
            payload = await self._client.a_get_by_commit(
                prompt_id, commit or "latest", branch=branch
            )
        self._load(payload)
        return self

    def push(self, *, branch: Optional[str] = None) -> PushPromptResult:
        """Push Prompt

        Creates a new commit for the prompt with the given `alias`, creating the
        prompt first when it does not exist. Send `text` for a text prompt or
        `messages` for a messages prompt, not both.
        """
        result = self._client.push(self._push_body(branch=branch))
        self.prompt_id = result.prompt_id
        self.hash = result.hash
        return result

    async def a_push(self, *, branch: Optional[str] = None) -> PushPromptResult:
        """Push Prompt

        Creates a new commit for the prompt with the given `alias`, creating the
        prompt first when it does not exist. Send `text` for a text prompt or
        `messages` for a messages prompt, not both.
        """
        result = await self._client.a_push(self._push_body(branch=branch))
        self.prompt_id = result.prompt_id
        self.hash = result.hash
        return result

    def _push_body(self, *, branch: Optional[str] = None) -> PushPromptRequest:
        if self.alias is None:
            raise ValueError("Set alias before calling push.")
        fields = {
            "alias": self.alias,
            "interpolation_type": self.interpolation_type,
            "model_settings": self.model_settings,
            "output_type": self.output_type,
            "output_schema": self.output_schema,
            "tools": self.tools,
            "branch": branch,
        }
        chosen = [
            name
            for name, value in (
                ("text", self.text),
                ("messages", self.messages),
            )
            if value is not None
        ]
        if len(chosen) != 1:
            raise ValueError(
                "Set exactly one of text, messages; "
                + (", ".join(chosen) if chosen else "none is set")
            )
        if self.text is not None:
            return PushTextPrompt(text=self.text, **fields)
        return PushMessagesPrompt(messages=self.messages, **fields)

    interpolate = interpolate_prompt

    def _load(self, payload: PromptPayload) -> None:
        # The payload is one branch of a union, so a field only the other
        # branch declares is absent rather than None.
        self.text = getattr(payload, "text", None)
        self.alias = getattr(payload, "alias", None)
        self.id = getattr(payload, "id", None)
        self.hash = getattr(payload, "hash", None)
        self.version = getattr(payload, "version", None)
        self.label = getattr(payload, "label", None)
        self.type = getattr(payload, "type", None)
        self.interpolation_type = getattr(payload, "interpolation_type", None)
        self.model_settings = getattr(payload, "model_settings", None)
        self.output_type = getattr(payload, "output_type", None)
        self.output_schema = getattr(payload, "output_schema", None)
        self.tools = getattr(payload, "tools", None)
        self.messages = getattr(payload, "messages", None)

    def _prompt_id(self) -> str:
        if self.prompt_id is None:
            raise ValueError(
                "This prompt has no prompt_id. Initialize it with "
                "client.prompt(prompt_id=...), or create it first."
            )
        return self.prompt_id

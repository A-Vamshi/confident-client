# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: dashboards).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List, Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import (
    CreateWidgetRequest,
    WidgetBucketMode,
    WidgetDimension,
    WidgetGranularity,
    WidgetLayout,
    WidgetLineConfig,
    WidgetMode,
    WidgetTopK,
    WidgetType,
    WidgetUnit,
)
from confidentai.dashboards.types import (
    UpdateWidgetRequest,
    Widget,
    WidgetQueryRequest,
    WidgetQueryResult,
    WidgetRef,
)
from confidentai.endpoints import Endpoints


class WidgetsOperations:
    _api: Api

    def create_widget(
        self,
        dashboard_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        type: Optional[WidgetType] = None,
        unit: Optional[WidgetUnit] = None,
        mode: Optional[WidgetMode] = None,
        bucket_mode: Optional[WidgetBucketMode] = None,
        dimension: Optional[WidgetDimension] = None,
        top_k: Optional[WidgetTopK] = None,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        layout: Optional[WidgetLayout] = None,
        lines: Optional[List[WidgetLineConfig]] = None
    ) -> WidgetRef:
        """Create Widget

        Adds a widget to a dashboard and returns its id. Send `layout` to place
        it yourself, or leave it out and Confident AI puts it on the first free
        row below the widgets already there.

        Args:
            dashboard_id: The id of the dashboard.
            name: The name shown as the widget's title.
            description: What the widget shows. Send null to leave it unset.
            type: The visualization to draw the widget as.
            unit: The unit the widget's values are labelled with.
            mode: How the widget aggregates its lines. `DIMENSION_SERIES`
                requires `dimension`.
            bucket_mode: How the query range is bucketed. Defaults to `SERIES`
                when omitted.
            dimension: The property to break the widget's data down by. Required
                when `mode` is `DIMENSION_SERIES`.
            top_k: Caps a dimension breakdown at its top values. Send null, or
                omit it, to plot every value.
            start_time: The start of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            end_time: The end of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            layout: Where the widget sits on the dashboard grid. Omit it, or
                send null, and Confident AI packs the widget into the first free
                space.
            lines: The series the widget plots.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.DASHBOARD_WIDGETS_ENDPOINT,
            response_schema=WidgetRef,
            request_schema=CreateWidgetRequest,
            body={
                "name": name,
                "description": description,
                "type": type,
                "unit": unit,
                "mode": mode,
                "bucketMode": bucket_mode,
                "dimension": dimension,
                "topK": top_k,
                "startTime": start_time,
                "endTime": end_time,
                "layout": layout,
                "lines": lines,
            },
            path={"dashboardId": dashboard_id},
        )

    async def a_create_widget(
        self,
        dashboard_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        type: Optional[WidgetType] = None,
        unit: Optional[WidgetUnit] = None,
        mode: Optional[WidgetMode] = None,
        bucket_mode: Optional[WidgetBucketMode] = None,
        dimension: Optional[WidgetDimension] = None,
        top_k: Optional[WidgetTopK] = None,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        layout: Optional[WidgetLayout] = None,
        lines: Optional[List[WidgetLineConfig]] = None
    ) -> WidgetRef:
        """Create Widget

        Adds a widget to a dashboard and returns its id. Send `layout` to place
        it yourself, or leave it out and Confident AI puts it on the first free
        row below the widgets already there.

        Args:
            dashboard_id: The id of the dashboard.
            name: The name shown as the widget's title.
            description: What the widget shows. Send null to leave it unset.
            type: The visualization to draw the widget as.
            unit: The unit the widget's values are labelled with.
            mode: How the widget aggregates its lines. `DIMENSION_SERIES`
                requires `dimension`.
            bucket_mode: How the query range is bucketed. Defaults to `SERIES`
                when omitted.
            dimension: The property to break the widget's data down by. Required
                when `mode` is `DIMENSION_SERIES`.
            top_k: Caps a dimension breakdown at its top values. Send null, or
                omit it, to plot every value.
            start_time: The start of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            end_time: The end of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            layout: Where the widget sits on the dashboard grid. Omit it, or
                send null, and Confident AI packs the widget into the first free
                space.
            lines: The series the widget plots.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.DASHBOARD_WIDGETS_ENDPOINT,
            response_schema=WidgetRef,
            request_schema=CreateWidgetRequest,
            body={
                "name": name,
                "description": description,
                "type": type,
                "unit": unit,
                "mode": mode,
                "bucketMode": bucket_mode,
                "dimension": dimension,
                "topK": top_k,
                "startTime": start_time,
                "endTime": end_time,
                "layout": layout,
                "lines": lines,
            },
            path={"dashboardId": dashboard_id},
        )

    def update_widget(
        self,
        dashboard_id: str,
        widget_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        type: Optional[WidgetType] = None,
        unit: Optional[WidgetUnit] = None,
        mode: Optional[WidgetMode] = None,
        bucket_mode: Optional[WidgetBucketMode] = None,
        dimension: Optional[WidgetDimension] = None,
        top_k: Optional[WidgetTopK] = None,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        layout: Optional[WidgetLayout] = None,
        lines: Optional[List[WidgetLineConfig]] = None
    ) -> Widget:
        """Update Widget

        Replaces a widget's configuration and returns it. This is a full
        replacement: every field you leave out is cleared, apart from `layout`
        and `lines`, which keep what is stored until you send them. Sending
        `lines` replaces the widget's lines outright, so the returned lines have
        new ids.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
            name: The name shown as the widget's title.
            description: What the widget shows. Send null to leave it unset.
            type: The visualization to draw the widget as.
            unit: The unit the widget's values are labelled with.
            mode: How the widget aggregates its lines. `DIMENSION_SERIES`
                requires `dimension`.
            bucket_mode: How the query range is bucketed. Defaults to `SERIES`
                when omitted.
            dimension: The property to break the widget's data down by. Required
                when `mode` is `DIMENSION_SERIES`.
            top_k: Caps a dimension breakdown at its top values. Send null, or
                omit it, to plot every value.
            start_time: The start of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            end_time: The end of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            layout: Where the widget sits on the dashboard grid. Omit it, or
                send null, and Confident AI packs the widget into the first free
                space.
            lines: The series the widget plots.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.DASHBOARD_WIDGET_ENDPOINT,
            response_schema=Widget,
            request_schema=UpdateWidgetRequest,
            body={
                "name": name,
                "description": description,
                "type": type,
                "unit": unit,
                "mode": mode,
                "bucketMode": bucket_mode,
                "dimension": dimension,
                "topK": top_k,
                "startTime": start_time,
                "endTime": end_time,
                "layout": layout,
                "lines": lines,
            },
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

    async def a_update_widget(
        self,
        dashboard_id: str,
        widget_id: str,
        name: str,
        *,
        description: Optional[str] = None,
        type: Optional[WidgetType] = None,
        unit: Optional[WidgetUnit] = None,
        mode: Optional[WidgetMode] = None,
        bucket_mode: Optional[WidgetBucketMode] = None,
        dimension: Optional[WidgetDimension] = None,
        top_k: Optional[WidgetTopK] = None,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        layout: Optional[WidgetLayout] = None,
        lines: Optional[List[WidgetLineConfig]] = None
    ) -> Widget:
        """Update Widget

        Replaces a widget's configuration and returns it. This is a full
        replacement: every field you leave out is cleared, apart from `layout`
        and `lines`, which keep what is stored until you send them. Sending
        `lines` replaces the widget's lines outright, so the returned lines have
        new ids.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
            name: The name shown as the widget's title.
            description: What the widget shows. Send null to leave it unset.
            type: The visualization to draw the widget as.
            unit: The unit the widget's values are labelled with.
            mode: How the widget aggregates its lines. `DIMENSION_SERIES`
                requires `dimension`.
            bucket_mode: How the query range is bucketed. Defaults to `SERIES`
                when omitted.
            dimension: The property to break the widget's data down by. Required
                when `mode` is `DIMENSION_SERIES`.
            top_k: Caps a dimension breakdown at its top values. Send null, or
                omit it, to plot every value.
            start_time: The start of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            end_time: The end of the widget's own time range, as an ISO 8601
                datetime. Send null to let the query decide the range.
            layout: Where the widget sits on the dashboard grid. Omit it, or
                send null, and Confident AI packs the widget into the first free
                space.
            lines: The series the widget plots.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.DASHBOARD_WIDGET_ENDPOINT,
            response_schema=Widget,
            request_schema=UpdateWidgetRequest,
            body={
                "name": name,
                "description": description,
                "type": type,
                "unit": unit,
                "mode": mode,
                "bucketMode": bucket_mode,
                "dimension": dimension,
                "topK": top_k,
                "startTime": start_time,
                "endTime": end_time,
                "layout": layout,
                "lines": lines,
            },
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

    def delete_widget(self, dashboard_id: str, widget_id: str) -> WidgetRef:
        """Delete Widget

        Removes a widget from a dashboard. A widget shown on other dashboards is
        only taken off this one; the last dashboard to drop it deletes it and
        its lines for good.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.DASHBOARD_WIDGET_ENDPOINT,
            response_schema=WidgetRef,
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

    async def a_delete_widget(
        self, dashboard_id: str, widget_id: str
    ) -> WidgetRef:
        """Delete Widget

        Removes a widget from a dashboard. A widget shown on other dashboards is
        only taken off this one; the last dashboard to drop it deletes it and
        its lines for good.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.DASHBOARD_WIDGET_ENDPOINT,
            response_schema=WidgetRef,
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

    def query_widget(
        self,
        dashboard_id: str,
        widget_id: str,
        *,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        granularity: Optional[WidgetGranularity] = None
    ) -> WidgetQueryResult:
        """Query Widget

        Computes the data behind one widget. A time range you send overrides the
        widget's own for this query only. Branch on `data.kind` to read the
        result: the widget's `type` and `mode` say how it is drawn, not how the
        payload is shaped.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
            start_time: The start of the range to compute over, as an ISO 8601
                datetime. Must be sent together with `endTime`, and overrides
                each widget's own range for this query only.
            end_time: The end of the range to compute over, as an ISO 8601
                datetime. Must be sent together with `startTime`, and must be
                later than it.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.DASHBOARD_WIDGET_QUERY_ENDPOINT,
            response_schema=WidgetQueryResult,
            request_schema=WidgetQueryRequest,
            body={
                "startTime": start_time,
                "endTime": end_time,
                "granularity": granularity,
            },
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

    async def a_query_widget(
        self,
        dashboard_id: str,
        widget_id: str,
        *,
        start_time: Optional[str] = None,
        end_time: Optional[str] = None,
        granularity: Optional[WidgetGranularity] = None
    ) -> WidgetQueryResult:
        """Query Widget

        Computes the data behind one widget. A time range you send overrides the
        widget's own for this query only. Branch on `data.kind` to read the
        result: the widget's `type` and `mode` say how it is drawn, not how the
        payload is shaped.

        Args:
            dashboard_id: The id of the dashboard the widget is on.
            widget_id: The id of the widget.
            start_time: The start of the range to compute over, as an ISO 8601
                datetime. Must be sent together with `endTime`, and overrides
                each widget's own range for this query only.
            end_time: The end of the range to compute over, as an ISO 8601
                datetime. Must be sent together with `startTime`, and must be
                later than it.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.DASHBOARD_WIDGET_QUERY_ENDPOINT,
            response_schema=WidgetQueryResult,
            request_schema=WidgetQueryRequest,
            body={
                "startTime": start_time,
                "endTime": end_time,
                "granularity": granularity,
            },
            path={"dashboardId": dashboard_id, "widgetId": widget_id},
        )

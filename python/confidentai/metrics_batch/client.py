# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: metrics-batch).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List

from confidentai.api import Api, HttpMethods
from confidentai.common.types import CreateMetricRequest, MetricList
from confidentai.endpoints import Endpoints
from confidentai.metrics_batch.types import CreateMetricsBatchRequest


class MetricsBatchClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def create_metrics_batch(
        self, metrics: List[CreateMetricRequest]
    ) -> MetricList:
        """Batch Create Metrics

        Creates several GEVAL metrics at once and returns the ones created.
        Metrics whose name already exists in the project are skipped. DAG
        metrics must be created one at a time.

        Args:
            metrics: The metrics to create. Names must be unique within the
                batch for the same `multiTurn`, and DAG metrics are not accepted
                here.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.METRICS_BATCH_ENDPOINT,
            response_schema=MetricList,
            request_schema=CreateMetricsBatchRequest,
            body={"metrics": metrics},
        )

    async def a_create_metrics_batch(
        self, metrics: List[CreateMetricRequest]
    ) -> MetricList:
        """Batch Create Metrics

        Creates several GEVAL metrics at once and returns the ones created.
        Metrics whose name already exists in the project are skipped. DAG
        metrics must be created one at a time.

        Args:
            metrics: The metrics to create. Names must be unique within the
                batch for the same `multiTurn`, and DAG metrics are not accepted
                here.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.METRICS_BATCH_ENDPOINT,
            response_schema=MetricList,
            request_schema=CreateMetricsBatchRequest,
            body={"metrics": metrics},
        )

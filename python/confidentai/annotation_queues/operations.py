# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: annotation-queues).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List, Optional

from confidentai.annotation_queues.types import (
    AnnotationQueue,
    AnnotationQueueList,
    AnnotationQueueRef,
    AnnotationQueueType,
    BatchAnnotateItem,
    BatchAnnotateRequest,
    BatchAnnotateResult,
    CreateAnnotationQueueRequest,
    UpdateAnnotationQueueRequest,
)
from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints


class AnnotationQueuesOperations:
    _api: Api

    def list(
        self,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None,
        type: Optional[AnnotationQueueType] = None,
        search_term: Optional[str] = None
    ) -> AnnotationQueueList:
        """List Annotation Queues

        Lists the annotation queues in your Confident AI project one page at a
        time, newest first. Each queue is returned as a summary with its
        progress; retrieve a queue by id for the per-reviewer breakdown.

        Args:
            page: The page of queues to return. Defaults to 1.
            page_size: The number of queues per page, at most 100. Defaults to
                25.
            type: Returns only queues holding this kind of item.
            search_term: Returns only queues whose name contains this text.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_QUEUES_ENDPOINT,
            response_schema=AnnotationQueueList,
            query={
                "page": page,
                "pageSize": page_size,
                "type": type,
                "searchTerm": search_term,
            },
        )

    async def a_list(
        self,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None,
        type: Optional[AnnotationQueueType] = None,
        search_term: Optional[str] = None
    ) -> AnnotationQueueList:
        """List Annotation Queues

        Lists the annotation queues in your Confident AI project one page at a
        time, newest first. Each queue is returned as a summary with its
        progress; retrieve a queue by id for the per-reviewer breakdown.

        Args:
            page: The page of queues to return. Defaults to 1.
            page_size: The number of queues per page, at most 100. Defaults to
                25.
            type: Returns only queues holding this kind of item.
            search_term: Returns only queues whose name contains this text.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_QUEUES_ENDPOINT,
            response_schema=AnnotationQueueList,
            query={
                "page": page,
                "pageSize": page_size,
                "type": type,
                "searchTerm": search_term,
            },
        )

    def create(
        self,
        name: str,
        type: AnnotationQueueType,
        *,
        form_id: Optional[str] = None
    ) -> AnnotationQueueRef:
        """Create Annotation Queue

        Creates an annotation queue in your Confident AI project and returns its
        id. The `type` you give it decides what can be added to it and cannot be
        changed afterwards.

        Args:
            name: The name of the queue, which must be unique in the project.
            form_id: The id of an annotation form in this project to ask of
                every item in the queue.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_QUEUES_ENDPOINT,
            response_schema=AnnotationQueueRef,
            request_schema=CreateAnnotationQueueRequest,
            body={"name": name, "type": type, "formId": form_id},
        )

    async def a_create(
        self,
        name: str,
        type: AnnotationQueueType,
        *,
        form_id: Optional[str] = None
    ) -> AnnotationQueueRef:
        """Create Annotation Queue

        Creates an annotation queue in your Confident AI project and returns its
        id. The `type` you give it decides what can be added to it and cannot be
        changed afterwards.

        Args:
            name: The name of the queue, which must be unique in the project.
            form_id: The id of an annotation form in this project to ask of
                every item in the queue.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_QUEUES_ENDPOINT,
            response_schema=AnnotationQueueRef,
            request_schema=CreateAnnotationQueueRequest,
            body={"name": name, "type": type, "formId": form_id},
        )

    def get(self, annotation_queue_id: str) -> AnnotationQueue:
        """Get Annotation Queue

        Retrieves an annotation queue by id from your Confident AI project, with
        how far through it your team is and how many items each reviewer holds.

        Args:
            annotation_queue_id: The id of the annotation queue.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueue,
            path={"annotationQueueId": annotation_queue_id},
        )

    async def a_get(self, annotation_queue_id: str) -> AnnotationQueue:
        """Get Annotation Queue

        Retrieves an annotation queue by id from your Confident AI project, with
        how far through it your team is and how many items each reviewer holds.

        Args:
            annotation_queue_id: The id of the annotation queue.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueue,
            path={"annotationQueueId": annotation_queue_id},
        )

    def update(
        self,
        annotation_queue_id: str,
        *,
        name: Optional[str] = None,
        form_id: Optional[str] = None
    ) -> AnnotationQueue:
        """Update Annotation Queue

        Renames an annotation queue or attaches a different annotation form to
        it, and returns the queue. Send `formId: null` to detach the current
        form.

        Args:
            annotation_queue_id: The id of the annotation queue.
            name: The new name of the queue, which must be unique in the
                project.
            form_id: The id of an annotation form to ask of every item in the
                queue. Send null to detach the current form.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueue,
            request_schema=UpdateAnnotationQueueRequest,
            body={"name": name, "formId": form_id},
            path={"annotationQueueId": annotation_queue_id},
        )

    async def a_update(
        self,
        annotation_queue_id: str,
        *,
        name: Optional[str] = None,
        form_id: Optional[str] = None
    ) -> AnnotationQueue:
        """Update Annotation Queue

        Renames an annotation queue or attaches a different annotation form to
        it, and returns the queue. Send `formId: null` to detach the current
        form.

        Args:
            annotation_queue_id: The id of the annotation queue.
            name: The new name of the queue, which must be unique in the
                project.
            form_id: The id of an annotation form to ask of every item in the
                queue. Send null to detach the current form.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueue,
            request_schema=UpdateAnnotationQueueRequest,
            body={"name": name, "formId": form_id},
            path={"annotationQueueId": annotation_queue_id},
        )

    def delete(self, annotation_queue_id: str) -> AnnotationQueueRef:
        """Delete Annotation Queue

        Permanently deletes an annotation queue and the items waiting in it.
        Annotations your team already recorded are kept.

        Args:
            annotation_queue_id: The id of the annotation queue.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueueRef,
            path={"annotationQueueId": annotation_queue_id},
        )

    async def a_delete(self, annotation_queue_id: str) -> AnnotationQueueRef:
        """Delete Annotation Queue

        Permanently deletes an annotation queue and the items waiting in it.
        Annotations your team already recorded are kept.

        Args:
            annotation_queue_id: The id of the annotation queue.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.ANNOTATION_QUEUE_ENDPOINT,
            response_schema=AnnotationQueueRef,
            path={"annotationQueueId": annotation_queue_id},
        )

    def batch_annotate_items(
        self,
        annotation_queue_id: str,
        items: List[BatchAnnotateItem],
        *,
        annotator_email: Optional[str] = None,
        mark_as_completed: Optional[bool] = None
    ) -> BatchAnnotateResult:
        """Batch Annotate Annotation Queue Items

        Records annotations for several items of one queue in a single call.
        Each entry is applied on its own, so the response carries one result per
        entry and a failure on one item does not stop the rest. `annotatorEmail`
        and `markAsCompleted` given at the top level apply to every entry that
        does not set its own.

        Args:
            annotation_queue_id: The id of the annotation queue.
            items: The items to annotate. Each is processed on its own, so one
                failure does not stop the rest.
            annotator_email: The email address credited for every entry that
                does not name its own annotator.
            mark_as_completed: Whether to mark the items annotated, for every
                entry that does not say otherwise. Defaults to true.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_QUEUE_BATCH_ANNOTATE_ENDPOINT,
            response_schema=BatchAnnotateResult,
            request_schema=BatchAnnotateRequest,
            body={
                "items": items,
                "annotatorEmail": annotator_email,
                "markAsCompleted": mark_as_completed,
            },
            path={"annotationQueueId": annotation_queue_id},
        )

    async def a_batch_annotate_items(
        self,
        annotation_queue_id: str,
        items: List[BatchAnnotateItem],
        *,
        annotator_email: Optional[str] = None,
        mark_as_completed: Optional[bool] = None
    ) -> BatchAnnotateResult:
        """Batch Annotate Annotation Queue Items

        Records annotations for several items of one queue in a single call.
        Each entry is applied on its own, so the response carries one result per
        entry and a failure on one item does not stop the rest. `annotatorEmail`
        and `markAsCompleted` given at the top level apply to every entry that
        does not set its own.

        Args:
            annotation_queue_id: The id of the annotation queue.
            items: The items to annotate. Each is processed on its own, so one
                failure does not stop the rest.
            annotator_email: The email address credited for every entry that
                does not name its own annotator.
            mark_as_completed: Whether to mark the items annotated, for every
                entry that does not say otherwise. Defaults to true.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ANNOTATION_QUEUE_BATCH_ANNOTATE_ENDPOINT,
            response_schema=BatchAnnotateResult,
            request_schema=BatchAnnotateRequest,
            body={
                "items": items,
                "annotatorEmail": annotator_email,
                "markAsCompleted": mark_as_completed,
            },
            path={"annotationQueueId": annotation_queue_id},
        )

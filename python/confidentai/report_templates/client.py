# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: report-templates).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import List, Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import (
    ScheduleIntervalUnit,
    ScheduleRecurrenceType,
)
from confidentai.endpoints import Endpoints
from confidentai.report_templates.types import (
    CreateReportTemplateRequest,
    ReportTemplate,
    ReportTemplateList,
    ReportTemplateRef,
    ReportTemplateSectionConfig,
    UpdateReportTemplateRequest,
)


class ReportTemplatesClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> ReportTemplateList:
        """List Report Templates

        Lists the report templates in your Confident AI project one page at a
        time, oldest first. Retrieve a single template for its cadence and its
        sections.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.REPORT_TEMPLATES_ENDPOINT,
            response_schema=ReportTemplateList,
            query={"page": page, "pageSize": page_size},
        )

    async def a_list(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> ReportTemplateList:
        """List Report Templates

        Lists the report templates in your Confident AI project one page at a
        time, oldest first. Retrieve a single template for its cadence and its
        sections.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.REPORT_TEMPLATES_ENDPOINT,
            response_schema=ReportTemplateList,
            query={"page": page, "pageSize": page_size},
        )

    def create(
        self,
        name: str,
        *,
        description: Optional[str] = None,
        template_sections: Optional[List[ReportTemplateSectionConfig]] = None,
        enabled: Optional[bool] = None,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None
    ) -> ReportTemplateRef:
        """Create Report Template

        Creates a report template in your Confident AI project and returns its
        id. The `description` is the question the report answers; send
        `templateSections` to fix its structure, and a cadence to control when
        it generates. Without a cadence it repeats every 1 day.

        Args:
            name: The template's name, also used as the report's title.
            description: The question the report should answer, written as a
                question. Supply this even when providing sections — it drives
                the single data retrieval that serves them all.
            template_sections: The report's exact sections, in render order.
                Omit it to let the generator choose the structure from
                `description`.
            enabled: Whether to start generating on the schedule. Defaults to
                true; send false to create the template without scheduling it.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.REPORT_TEMPLATES_ENDPOINT,
            response_schema=ReportTemplateRef,
            request_schema=CreateReportTemplateRequest,
            body={
                "name": name,
                "description": description,
                "templateSections": template_sections,
                "enabled": enabled,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
            },
        )

    async def a_create(
        self,
        name: str,
        *,
        description: Optional[str] = None,
        template_sections: Optional[List[ReportTemplateSectionConfig]] = None,
        enabled: Optional[bool] = None,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None
    ) -> ReportTemplateRef:
        """Create Report Template

        Creates a report template in your Confident AI project and returns its
        id. The `description` is the question the report answers; send
        `templateSections` to fix its structure, and a cadence to control when
        it generates. Without a cadence it repeats every 1 day.

        Args:
            name: The template's name, also used as the report's title.
            description: The question the report should answer, written as a
                question. Supply this even when providing sections — it drives
                the single data retrieval that serves them all.
            template_sections: The report's exact sections, in render order.
                Omit it to let the generator choose the structure from
                `description`.
            enabled: Whether to start generating on the schedule. Defaults to
                true; send false to create the template without scheduling it.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.REPORT_TEMPLATES_ENDPOINT,
            response_schema=ReportTemplateRef,
            request_schema=CreateReportTemplateRequest,
            body={
                "name": name,
                "description": description,
                "templateSections": template_sections,
                "enabled": enabled,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
            },
        )

    def get(self, report_template_id: str) -> ReportTemplate:
        """Get Report Template

        Retrieves a report template by id, with its generation cadence and all
        of its section definitions.

        Args:
            report_template_id: The id of the report template.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplate,
            path={"reportTemplateId": report_template_id},
        )

    async def a_get(self, report_template_id: str) -> ReportTemplate:
        """Get Report Template

        Retrieves a report template by id, with its generation cadence and all
        of its section definitions.

        Args:
            report_template_id: The id of the report template.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplate,
            path={"reportTemplateId": report_template_id},
        )

    def update(
        self,
        report_template_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        template_sections: Optional[List[ReportTemplateSectionConfig]] = None,
        enabled: Optional[bool] = None,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None
    ) -> ReportTemplate:
        """Update Report Template

        Updates a report template and returns it. Only the fields you send are
        changed, and `templateSections` replaces the whole list. Set `enabled`
        to false to pause generation, or send cadence fields to retime it.

        Args:
            report_template_id: The id of the report template.
            name: The template's new name, also used as the report's title.
            description: The question the report should answer, which drives
                what data the generator retrieves. Send null to clear it.
            template_sections: The report's exact sections, in render order. The
                list replaces the template's current sections, so a section you
                leave out is removed; omit the field to leave them alone.
            enabled: Whether scheduled generation runs. False pauses it while
                keeping past reports readable. A schedule that has hit its
                `maxRuns` or `endAt` can only be re-enabled by a request that
                also raises or clears them.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplate,
            request_schema=UpdateReportTemplateRequest,
            body={
                "name": name,
                "description": description,
                "templateSections": template_sections,
                "enabled": enabled,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
            },
            path={"reportTemplateId": report_template_id},
        )

    async def a_update(
        self,
        report_template_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        template_sections: Optional[List[ReportTemplateSectionConfig]] = None,
        enabled: Optional[bool] = None,
        recurrence: Optional[ScheduleRecurrenceType] = None,
        repeat_every: Optional[int] = None,
        repeat_unit: Optional[ScheduleIntervalUnit] = None,
        start_at: Optional[str] = None,
        max_runs: Optional[int] = None,
        end_at: Optional[str] = None
    ) -> ReportTemplate:
        """Update Report Template

        Updates a report template and returns it. Only the fields you send are
        changed, and `templateSections` replaces the whole list. Set `enabled`
        to false to pause generation, or send cadence fields to retime it.

        Args:
            report_template_id: The id of the report template.
            name: The template's new name, also used as the report's title.
            description: The question the report should answer, which drives
                what data the generator retrieves. Send null to clear it.
            template_sections: The report's exact sections, in render order. The
                list replaces the template's current sections, so a section you
                leave out is removed; omit the field to leave them alone.
            enabled: Whether scheduled generation runs. False pauses it while
                keeping past reports readable. A schedule that has hit its
                `maxRuns` or `endAt` can only be re-enabled by a request that
                also raises or clears them.
            repeat_every: How many `repeatUnit`s apart the runs are, for an
                INTERVAL schedule. Send null to clear it.
            repeat_unit: The unit `repeatEvery` counts, for an INTERVAL
                schedule. Send null to clear it.
            start_at: When the schedule first runs, as an ISO 8601 datetime.
                Send null to start it immediately.
            max_runs: How many times the schedule runs before it stops. Send
                null to let it run indefinitely.
            end_at: When the schedule stops running, as an ISO 8601 datetime.
                Send null to leave it open-ended.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplate,
            request_schema=UpdateReportTemplateRequest,
            body={
                "name": name,
                "description": description,
                "templateSections": template_sections,
                "enabled": enabled,
                "recurrence": recurrence,
                "repeatEvery": repeat_every,
                "repeatUnit": repeat_unit,
                "startAt": start_at,
                "maxRuns": max_runs,
                "endAt": end_at,
            },
            path={"reportTemplateId": report_template_id},
        )

    def delete(self, report_template_id: str) -> ReportTemplateRef:
        """Delete Report Template

        Permanently deletes a report template and the schedule that generates
        it. This cannot be undone, and every report it generated becomes
        unreachable — set `enabled` to false instead to pause generation while
        keeping past reports readable.

        Args:
            report_template_id: The id of the report template.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplateRef,
            path={"reportTemplateId": report_template_id},
        )

    async def a_delete(self, report_template_id: str) -> ReportTemplateRef:
        """Delete Report Template

        Permanently deletes a report template and the schedule that generates
        it. This cannot be undone, and every report it generated becomes
        unreachable — set `enabled` to false instead to pause generation while
        keeping past reports readable.

        Args:
            report_template_id: The id of the report template.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.REPORT_TEMPLATE_ENDPOINT,
            response_schema=ReportTemplateRef,
            path={"reportTemplateId": report_template_id},
        )

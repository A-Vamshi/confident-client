# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: personas).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.personas.types import (
    CreatePersonaRequest,
    Persona,
    PersonaList,
    PersonaRef,
    UpdatePersonaRequest,
)


class PersonasClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list_personas(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> PersonaList:
        """List Personas

        Lists the personas in your Confident AI project one page at a time,
        newest first. Each persona is returned with just its id and name —
        enough to pick one for a multi-turn golden; retrieve it by id to read
        the characteristics the simulator plays it with.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.PERSONAS_ENDPOINT,
            response_schema=PersonaList,
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_personas(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> PersonaList:
        """List Personas

        Lists the personas in your Confident AI project one page at a time,
        newest first. Each persona is returned with just its id and name —
        enough to pick one for a multi-turn golden; retrieve it by id to read
        the characteristics the simulator plays it with.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.PERSONAS_ENDPOINT,
            response_schema=PersonaList,
            query={"page": page, "pageSize": page_size},
        )

    def create_persona(self, name: str, characteristics: str) -> PersonaRef:
        """Create Persona

        Creates a persona in your Confident AI project and returns its id. A
        persona is the character the simulated user plays in a multi-turn
        conversation, so defining one here is what lets many goldens share the
        same user instead of restating the same description on every row. Names
        are unique within a project; reusing one returns a 409.

        Args:
            name: The name of the persona, unique within the project. It is the
                label the persona is picked by wherever a golden is given one,
                so make it recognisable on its own — 'Frustrated support caller'
                rather than 'Persona 2'.
            characteristics: How this persona behaves in a conversation: tone,
                temperament, patience, how much they volunteer, how they phrase
                things. The simulator reads it as the standing instruction for
                the user side of every turn, so describe a person rather than a
                task — what the conversation is about and when it is finished
                come from the golden's scenario and expected outcome, not from
                here.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.PERSONAS_ENDPOINT,
            response_schema=PersonaRef,
            request_schema=CreatePersonaRequest,
            body={"name": name, "characteristics": characteristics},
        )

    async def a_create_persona(
        self, name: str, characteristics: str
    ) -> PersonaRef:
        """Create Persona

        Creates a persona in your Confident AI project and returns its id. A
        persona is the character the simulated user plays in a multi-turn
        conversation, so defining one here is what lets many goldens share the
        same user instead of restating the same description on every row. Names
        are unique within a project; reusing one returns a 409.

        Args:
            name: The name of the persona, unique within the project. It is the
                label the persona is picked by wherever a golden is given one,
                so make it recognisable on its own — 'Frustrated support caller'
                rather than 'Persona 2'.
            characteristics: How this persona behaves in a conversation: tone,
                temperament, patience, how much they volunteer, how they phrase
                things. The simulator reads it as the standing instruction for
                the user side of every turn, so describe a person rather than a
                task — what the conversation is about and when it is finished
                come from the golden's scenario and expected outcome, not from
                here.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.PERSONAS_ENDPOINT,
            response_schema=PersonaRef,
            request_schema=CreatePersonaRequest,
            body={"name": name, "characteristics": characteristics},
        )

    def get_persona(self, persona_id: str) -> Persona:
        """Get Persona

        Retrieves a persona by id, including the characteristics the simulator
        plays it with. This is the text to read when a simulated conversation
        did not sound the way you expected: the persona supplies who is talking,
        while the golden it is attached to supplies what they want.

        Args:
            persona_id: The id of the persona.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=Persona,
            path={"personaId": persona_id},
        )

    async def a_get_persona(self, persona_id: str) -> Persona:
        """Get Persona

        Retrieves a persona by id, including the characteristics the simulator
        plays it with. This is the text to read when a simulated conversation
        did not sound the way you expected: the persona supplies who is talking,
        while the golden it is attached to supplies what they want.

        Args:
            persona_id: The id of the persona.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=Persona,
            path={"personaId": persona_id},
        )

    def update_persona(
        self,
        persona_id: str,
        *,
        name: Optional[str] = None,
        characteristics: Optional[str] = None
    ) -> Persona:
        """Update Persona

        Renames a persona or rewrites its characteristics, and returns it. Every
        golden already pointing at this persona picks the new text up on its
        next simulation, so an edit changes how future conversations are played
        rather than conversations already simulated. Names are unique within a
        project; taking another persona's name returns a 409.

        Args:
            persona_id: The id of the persona.
            name: The name of the persona, unique within the project. Omit it to
                keep the current name.
            characteristics: How this persona behaves in a conversation, as the
                simulator reads it on every user turn. The text replaces the
                stored one outright rather than being appended to, so send the
                whole description. Omit it to keep the current one.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=Persona,
            request_schema=UpdatePersonaRequest,
            body={"name": name, "characteristics": characteristics},
            path={"personaId": persona_id},
        )

    async def a_update_persona(
        self,
        persona_id: str,
        *,
        name: Optional[str] = None,
        characteristics: Optional[str] = None
    ) -> Persona:
        """Update Persona

        Renames a persona or rewrites its characteristics, and returns it. Every
        golden already pointing at this persona picks the new text up on its
        next simulation, so an edit changes how future conversations are played
        rather than conversations already simulated. Names are unique within a
        project; taking another persona's name returns a 409.

        Args:
            persona_id: The id of the persona.
            name: The name of the persona, unique within the project. Omit it to
                keep the current name.
            characteristics: How this persona behaves in a conversation, as the
                simulator reads it on every user turn. The text replaces the
                stored one outright rather than being appended to, so send the
                whole description. Omit it to keep the current one.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=Persona,
            request_schema=UpdatePersonaRequest,
            body={"name": name, "characteristics": characteristics},
            path={"personaId": persona_id},
        )

    def delete_persona(self, persona_id: str) -> PersonaRef:
        """Delete Persona

        Permanently deletes a persona. Goldens that used it are kept and simply
        lose their persona, so their next simulation runs with no character
        attached until you give them another one.

        Args:
            persona_id: The id of the persona.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=PersonaRef,
            path={"personaId": persona_id},
        )

    async def a_delete_persona(self, persona_id: str) -> PersonaRef:
        """Delete Persona

        Permanently deletes a persona. Goldens that used it are kept and simply
        lose their persona, so their next simulation runs with no character
        attached until you give them another one.

        Args:
            persona_id: The id of the persona.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.PERSONA_ENDPOINT,
            response_schema=PersonaRef,
            path={"personaId": persona_id},
        )

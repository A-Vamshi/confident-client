# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: organization).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import MemberRef, UpdateMemberRoleRequest
from confidentai.endpoints import Endpoints
from confidentai.organization.types import (
    OrganizationMember,
    OrganizationMemberList,
)


class MembersOperations:
    _api: Api

    def list_members(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> OrganizationMemberList:
        """List Organization Members

        Lists the members of your organization one page at a time, each with the
        organization role that decides what they can do. Membership here is what
        grants access to the organization itself; a member still has to be added
        to a project before they can see that project's data, so every project's
        member list is drawn from this one.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of members per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_MEMBERS_ENDPOINT,
            response_schema=OrganizationMemberList,
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_members(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> OrganizationMemberList:
        """List Organization Members

        Lists the members of your organization one page at a time, each with the
        organization role that decides what they can do. Membership here is what
        grants access to the organization itself; a member still has to be added
        to a project before they can see that project's data, so every project's
        member list is drawn from this one.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of members per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_MEMBERS_ENDPOINT,
            response_schema=OrganizationMemberList,
            query={"page": page, "pageSize": page_size},
        )

    def update_member_role(
        self, user_id: str, role_id: str
    ) -> OrganizationMember:
        """Update Organization Member Role

        Replaces a member's organization role, which changes what they may do
        across the organization from their next request onwards. Assigning the
        `Owner` role transfers ownership: the member becomes Owner and the
        previous Owner is demoted to `Admin` in the same transaction. That is
        the only way the Owner's role changes — moving the Owner onto any other
        role directly is refused. A role id belonging to another organization is
        rejected, and the member's project roles are left untouched.

        Args:
            user_id: The id of the user whose organization membership to change.
            role_id: The id of the role to assign. It must be a Confident AI
                built-in role or one of the roles the organization or project
                owns.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_MEMBER_ENDPOINT,
            response_schema=OrganizationMember,
            request_schema=UpdateMemberRoleRequest,
            body={"roleId": role_id},
            path={"userId": user_id},
        )

    async def a_update_member_role(
        self, user_id: str, role_id: str
    ) -> OrganizationMember:
        """Update Organization Member Role

        Replaces a member's organization role, which changes what they may do
        across the organization from their next request onwards. Assigning the
        `Owner` role transfers ownership: the member becomes Owner and the
        previous Owner is demoted to `Admin` in the same transaction. That is
        the only way the Owner's role changes — moving the Owner onto any other
        role directly is refused. A role id belonging to another organization is
        rejected, and the member's project roles are left untouched.

        Args:
            user_id: The id of the user whose organization membership to change.
            role_id: The id of the role to assign. It must be a Confident AI
                built-in role or one of the roles the organization or project
                owns.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_MEMBER_ENDPOINT,
            response_schema=OrganizationMember,
            request_schema=UpdateMemberRoleRequest,
            body={"roleId": role_id},
            path={"userId": user_id},
        )

    def remove_member(self, user_id: str) -> MemberRef:
        """Remove Organization Member

        Revokes a member's access to the organization and to everything inside
        it: they are detached from the organization, disconnected from every
        project in it, their organization and project roles are deleted, and any
        invitation still outstanding for their email address is cleared. The
        Owner cannot be removed, so transfer ownership first. Removal is not
        reversible through this endpoint — the only way back is a fresh
        invitation, which returns them with no project access. The user's own
        account and the records they created are kept.

        Args:
            user_id: The id of the user whose organization membership to change.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_MEMBER_ENDPOINT,
            response_schema=MemberRef,
            path={"userId": user_id},
        )

    async def a_remove_member(self, user_id: str) -> MemberRef:
        """Remove Organization Member

        Revokes a member's access to the organization and to everything inside
        it: they are detached from the organization, disconnected from every
        project in it, their organization and project roles are deleted, and any
        invitation still outstanding for their email address is cleared. The
        Owner cannot be removed, so transfer ownership first. Removal is not
        reversible through this endpoint — the only way back is a fresh
        invitation, which returns them with no project access. The user's own
        account and the records they created are kept.

        Args:
            user_id: The id of the user whose organization membership to change.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_MEMBER_ENDPOINT,
            response_schema=MemberRef,
            path={"userId": user_id},
        )

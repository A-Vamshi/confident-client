# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: organization).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.organization.types import (
    AssessGovernanceControlResult,
    CreatableGovernanceControlType,
    CreateGovernanceControlRequest,
    GovernanceControl,
    GovernanceControlActivity,
    GovernanceControlAssessmentList,
    GovernanceControlList,
    GovernanceControlPreDeploymentConfig,
    GovernanceControlRef,
    GovernanceControlRuntimeConfig,
    GovernanceControlType,
    UpdateGovernanceControlRequest,
)


class GovernanceControlsOperations:
    _api: Api

    def list_governance_controls(
        self,
        *,
        type: Optional[GovernanceControlType] = None,
        activity: Optional[GovernanceControlActivity] = None,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> GovernanceControlList:
        """List Governance Controls

        Lists your organization's governance controls, newest created first,
        each with its health across the projects it governs. A control's
        definition is not included — read its versions for that. Health is
        computed from the latest verdict per governed project, so it reflects
        the current state rather than the whole assessment history.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of controls per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROLS_ENDPOINT,
            response_schema=GovernanceControlList,
            query={
                "type": type,
                "activity": activity,
                "page": page,
                "pageSize": page_size,
            },
        )

    async def a_list_governance_controls(
        self,
        *,
        type: Optional[GovernanceControlType] = None,
        activity: Optional[GovernanceControlActivity] = None,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> GovernanceControlList:
        """List Governance Controls

        Lists your organization's governance controls, newest created first,
        each with its health across the projects it governs. A control's
        definition is not included — read its versions for that. Health is
        computed from the latest verdict per governed project, so it reflects
        the current state rather than the whole assessment history.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of controls per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROLS_ENDPOINT,
            response_schema=GovernanceControlList,
            query={
                "type": type,
                "activity": activity,
                "page": page,
                "pageSize": page_size,
            },
        )

    def create_governance_control(
        self,
        name: str,
        type: CreatableGovernanceControlType,
        *,
        description: Optional[str] = None,
        runtime_config: Optional[GovernanceControlRuntimeConfig] = None,
        pre_deployment_config: Optional[
            GovernanceControlPreDeploymentConfig
        ] = None,
        governance_policy_id: Optional[str] = None
    ) -> GovernanceControlRef:
        """Create Governance Control

        Creates a governance control and returns its id. Supplying the config
        that matches the control's `type` snapshots its first version in the
        same call; omitting it creates the control with no definition, which
        assesses as ERROR until you add a version. Operational controls are
        seeded by Confident AI and cannot be created here. A new control governs
        nothing until a governance policy holds it.

        Args:
            name: The name of the control, unique within your organization.
            description: What the control checks and why. Send null to leave it
                unset.
            governance_policy_id: Accepted but not acted on: the control is
                created unattached whether or not you send it. Attach it through
                the governance policy's own controls endpoint.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROLS_ENDPOINT,
            response_schema=GovernanceControlRef,
            request_schema=CreateGovernanceControlRequest,
            body={
                "name": name,
                "type": type,
                "description": description,
                "runtimeConfig": runtime_config,
                "preDeploymentConfig": pre_deployment_config,
                "governancePolicyId": governance_policy_id,
            },
        )

    async def a_create_governance_control(
        self,
        name: str,
        type: CreatableGovernanceControlType,
        *,
        description: Optional[str] = None,
        runtime_config: Optional[GovernanceControlRuntimeConfig] = None,
        pre_deployment_config: Optional[
            GovernanceControlPreDeploymentConfig
        ] = None,
        governance_policy_id: Optional[str] = None
    ) -> GovernanceControlRef:
        """Create Governance Control

        Creates a governance control and returns its id. Supplying the config
        that matches the control's `type` snapshots its first version in the
        same call; omitting it creates the control with no definition, which
        assesses as ERROR until you add a version. Operational controls are
        seeded by Confident AI and cannot be created here. A new control governs
        nothing until a governance policy holds it.

        Args:
            name: The name of the control, unique within your organization.
            description: What the control checks and why. Send null to leave it
                unset.
            governance_policy_id: Accepted but not acted on: the control is
                created unattached whether or not you send it. Attach it through
                the governance policy's own controls endpoint.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROLS_ENDPOINT,
            response_schema=GovernanceControlRef,
            request_schema=CreateGovernanceControlRequest,
            body={
                "name": name,
                "type": type,
                "description": description,
                "runtimeConfig": runtime_config,
                "preDeploymentConfig": pre_deployment_config,
                "governancePolicyId": governance_policy_id,
            },
        )

    def get_governance_control(self, control_id: str) -> GovernanceControl:
        """Get Governance Control

        Retrieves a single governance control with its health across the
        projects it governs and when it was last assessed. The rule it evaluates
        is not returned here — list the control's versions to read its
        definition.

        Args:
            control_id: The id of the governance control.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControl,
            path={"controlId": control_id},
        )

    async def a_get_governance_control(
        self, control_id: str
    ) -> GovernanceControl:
        """Get Governance Control

        Retrieves a single governance control with its health across the
        projects it governs and when it was last assessed. The rule it evaluates
        is not returned here — list the control's versions to read its
        definition.

        Args:
            control_id: The id of the governance control.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControl,
            path={"controlId": control_id},
        )

    def update_governance_control(
        self,
        control_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None
    ) -> GovernanceControl:
        """Update Governance Control

        Updates a governance control's name or description and returns it as
        stored. Both live on the control itself rather than on a version, so
        this does not snapshot a new version and does not change what the
        control checks — append a version for that. An operational control's
        name and description come from Confident AI's registry and cannot be
        edited.

        Args:
            control_id: The id of the governance control.
            name: The name of the control, unique within your organization.
            description: What the control checks and why. Send null to clear it.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControl,
            request_schema=UpdateGovernanceControlRequest,
            body={"name": name, "description": description},
            path={"controlId": control_id},
        )

    async def a_update_governance_control(
        self,
        control_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None
    ) -> GovernanceControl:
        """Update Governance Control

        Updates a governance control's name or description and returns it as
        stored. Both live on the control itself rather than on a version, so
        this does not snapshot a new version and does not change what the
        control checks — append a version for that. An operational control's
        name and description come from Confident AI's registry and cannot be
        edited.

        Args:
            control_id: The id of the governance control.
            name: The name of the control, unique within your organization.
            description: What the control checks and why. Send null to clear it.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControl,
            request_schema=UpdateGovernanceControlRequest,
            body={"name": name, "description": description},
            path={"controlId": control_id},
        )

    def delete_governance_control(
        self, control_id: str
    ) -> GovernanceControlRef:
        """Delete Governance Control

        Permanently deletes a governance control, every version of its
        definition, and every verdict recorded against it. Any policy holding
        the control loses it and stops applying that check. **Warning:** This
        action cannot be undone. To stop a control gating one policy without
        deleting it, remove it from that policy instead.

        Args:
            control_id: The id of the governance control.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControlRef,
            path={"controlId": control_id},
        )

    async def a_delete_governance_control(
        self, control_id: str
    ) -> GovernanceControlRef:
        """Delete Governance Control

        Permanently deletes a governance control, every version of its
        definition, and every verdict recorded against it. Any policy holding
        the control loses it and stops applying that check. **Warning:** This
        action cannot be undone. To stop a control gating one policy without
        deleting it, remove it from that policy instead.

        Args:
            control_id: The id of the governance control.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ENDPOINT,
            response_schema=GovernanceControlRef,
            path={"controlId": control_id},
        )

    def assess_governance_control(
        self, control_id: str
    ) -> AssessGovernanceControlResult:
        """Assess Governance Control

        Runs a governance control now against every project it governs and
        records a fresh verdict per project, rather than waiting for the
        scheduled sweep. A project is governed when its policy holds the control
        or extends a base policy that holds it, and the run always uses the
        control's current version. Each project resolves to one verdict. A
        RUNTIME control aggregates its data model over the trailing 24 hours and
        returns FAIL when the result sits on the threshold's `direction` side of
        it, PASS when it does not, and NO_DATA when the window produced nothing
        to measure. A pre-deployment control returns PASS when the newest
        completed run in its window satisfies the control's filters, FAIL when
        it does not, and NO_DATA when there is no such run. An OPERATIONAL
        control returns whatever the platform check finds. Any control that is
        not fully configured, or whose aggregation does not fit its data model,
        returns ERROR with the reason on the assessment. Every verdict is
        stored, so it becomes the project's current status for this control and
        appears in the control's assessment history. Assessments are append-only
        — running this repeatedly adds rows rather than replacing them. It
        consumes evaluation resources proportional to the number of governed
        projects. A control attached to no policy, or one with no version yet,
        governs nothing and returns zero counts.

        Args:
            control_id: The id of the governance control.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ASSESS_ENDPOINT,
            response_schema=AssessGovernanceControlResult,
            path={"controlId": control_id},
        )

    async def a_assess_governance_control(
        self, control_id: str
    ) -> AssessGovernanceControlResult:
        """Assess Governance Control

        Runs a governance control now against every project it governs and
        records a fresh verdict per project, rather than waiting for the
        scheduled sweep. A project is governed when its policy holds the control
        or extends a base policy that holds it, and the run always uses the
        control's current version. Each project resolves to one verdict. A
        RUNTIME control aggregates its data model over the trailing 24 hours and
        returns FAIL when the result sits on the threshold's `direction` side of
        it, PASS when it does not, and NO_DATA when the window produced nothing
        to measure. A pre-deployment control returns PASS when the newest
        completed run in its window satisfies the control's filters, FAIL when
        it does not, and NO_DATA when there is no such run. An OPERATIONAL
        control returns whatever the platform check finds. Any control that is
        not fully configured, or whose aggregation does not fit its data model,
        returns ERROR with the reason on the assessment. Every verdict is
        stored, so it becomes the project's current status for this control and
        appears in the control's assessment history. Assessments are append-only
        — running this repeatedly adds rows rather than replacing them. It
        consumes evaluation resources proportional to the number of governed
        projects. A control attached to no policy, or one with no version yet,
        governs nothing and returns zero counts.

        Args:
            control_id: The id of the governance control.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ASSESS_ENDPOINT,
            response_schema=AssessGovernanceControlResult,
            path={"controlId": control_id},
        )

    def list_governance_control_assessments(
        self,
        control_id: str,
        *,
        version: Optional[str] = None,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> GovernanceControlAssessmentList:
        """List Governance Assessments

        Lists a governance control's recorded verdicts, one per project per run,
        ordered newest recorded first with ties broken by assessment id.
        Verdicts belong to the version of the definition they were computed
        against, so they are read one version at a time: send `version` to read
        a past version's verdicts, or omit it to read the current version, which
        the response echoes back. There is no time window — every verdict ever
        recorded against that version is paginated here, so a control assessed
        daily across five projects returns five rows per day rather than a
        single current state. The newest verdict for a project is that project's
        current status; everything older is history. A control with no versions
        yet is a 404 rather than an empty list.

        Args:
            control_id: The id of the governance control.
            version: The `version` label of the control version to read verdicts
                for. Omit it to read the current version, which is the one with
                the highest sequence.
            page: The page to return. Defaults to 1.
            page_size: The number of assessments per page, at most 100. Defaults
                to 25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ASSESSMENTS_ENDPOINT,
            response_schema=GovernanceControlAssessmentList,
            path={"controlId": control_id},
            query={"version": version, "page": page, "pageSize": page_size},
        )

    async def a_list_governance_control_assessments(
        self,
        control_id: str,
        *,
        version: Optional[str] = None,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> GovernanceControlAssessmentList:
        """List Governance Assessments

        Lists a governance control's recorded verdicts, one per project per run,
        ordered newest recorded first with ties broken by assessment id.
        Verdicts belong to the version of the definition they were computed
        against, so they are read one version at a time: send `version` to read
        a past version's verdicts, or omit it to read the current version, which
        the response echoes back. There is no time window — every verdict ever
        recorded against that version is paginated here, so a control assessed
        daily across five projects returns five rows per day rather than a
        single current state. The newest verdict for a project is that project's
        current status; everything older is history. A control with no versions
        yet is a 404 rather than an empty list.

        Args:
            control_id: The id of the governance control.
            version: The `version` label of the control version to read verdicts
                for. Omit it to read the current version, which is the one with
                the highest sequence.
            page: The page to return. Defaults to 1.
            page_size: The number of assessments per page, at most 100. Defaults
                to 25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_GOVERNANCE_CONTROL_ASSESSMENTS_ENDPOINT,
            response_schema=GovernanceControlAssessmentList,
            path={"controlId": control_id},
            query={"version": version, "page": page, "pageSize": page_size},
        )

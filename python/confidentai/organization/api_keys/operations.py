# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: organization).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.common.types import (
    ApiKey,
    ApiKeyList,
    ApiKeyRef,
    CreateApiKeyRequest,
    CreatedApiKey,
    RotateApiKeyRequest,
    RotatedApiKey,
    UpdateApiKeyRequest,
)
from confidentai.endpoints import Endpoints


class ApiKeysOperations:
    _api: Api

    def list_organization_api_keys(self) -> ApiKeyList:
        """List Organization API Keys

        Lists every organization-scoped API key in your Confident AI
        organization, newest first. Each key's `value` is masked (only its last
        six characters are shown) — the full value is only ever returned once,
        by the response that issues it. A rotation whose grace period has
        already run out is completed before the list is read, so a `shadowValue`
        here is always still in flight.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_API_KEYS_ENDPOINT,
            response_schema=ApiKeyList,
        )

    async def a_list_organization_api_keys(self) -> ApiKeyList:
        """List Organization API Keys

        Lists every organization-scoped API key in your Confident AI
        organization, newest first. Each key's `value` is masked (only its last
        six characters are shown) — the full value is only ever returned once,
        by the response that issues it. A rotation whose grace period has
        already run out is completed before the list is read, so a `shadowValue`
        here is always still in flight.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_API_KEYS_ENDPOINT,
            response_schema=ApiKeyList,
        )

    def create_organization_api_key(
        self, name: str, *, expires_in_days: Optional[int] = None
    ) -> CreatedApiKey:
        """Create Organization API Key

        Mints a new organization-scoped API key. The full `value` is returned
        **exactly once**, in this response, and can never be retrieved again —
        store it securely. An organization key administers the organization and
        its projects; it is not the key an application sends traces with.

        Args:
            name: A label for the key, shown on the Confident AI platform.
            expires_in_days: How long the key lasts, in days from now — a
                duration, not a date. Confident AI turns it into the `expiresAt`
                instant on the key. Omit it for a key that never expires.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_API_KEYS_ENDPOINT,
            response_schema=CreatedApiKey,
            request_schema=CreateApiKeyRequest,
            body={"name": name, "expiresInDays": expires_in_days},
        )

    async def a_create_organization_api_key(
        self, name: str, *, expires_in_days: Optional[int] = None
    ) -> CreatedApiKey:
        """Create Organization API Key

        Mints a new organization-scoped API key. The full `value` is returned
        **exactly once**, in this response, and can never be retrieved again —
        store it securely. An organization key administers the organization and
        its projects; it is not the key an application sends traces with.

        Args:
            name: A label for the key, shown on the Confident AI platform.
            expires_in_days: How long the key lasts, in days from now — a
                duration, not a date. Confident AI turns it into the `expiresAt`
                instant on the key. Omit it for a key that never expires.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_API_KEYS_ENDPOINT,
            response_schema=CreatedApiKey,
            request_schema=CreateApiKeyRequest,
            body={"name": name, "expiresInDays": expires_in_days},
        )

    def get_organization_api_key(self, api_key_id: str) -> ApiKey:
        """Get Organization API Key

        Retrieves one organization-scoped API key by id. Its `value` is masked —
        the full value is only ever returned once, by the response that issues
        it. A `rotatesAt` in the past means the grace period is over and the
        outgoing value is already rejected on authentication, even though this
        endpoint still shows it; listing the keys completes the rotation.

        Args:
            api_key_id: The id of the API key.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKey,
            path={"apiKeyId": api_key_id},
        )

    async def a_get_organization_api_key(self, api_key_id: str) -> ApiKey:
        """Get Organization API Key

        Retrieves one organization-scoped API key by id. Its `value` is masked —
        the full value is only ever returned once, by the response that issues
        it. A `rotatesAt` in the past means the grace period is over and the
        outgoing value is already rejected on authentication, even though this
        endpoint still shows it; listing the keys completes the rotation.

        Args:
            api_key_id: The id of the API key.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKey,
            path={"apiKeyId": api_key_id},
        )

    def update_organization_api_key(
        self, api_key_id: str, valid: bool
    ) -> ApiKey:
        """Update Organization API Key

        Activates or deactivates an organization-scoped API key. A deactivated
        key is rejected on authentication from the next request onwards; its
        value is unchanged and reactivating it brings the same value back.
        Rotating is the only way to change the value.

        Args:
            api_key_id: The id of the API key.
            valid: Send false to deactivate the key, true to reactivate it. A
                deactivated key is rejected on every request, and deactivating
                one takes effect immediately.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKey,
            request_schema=UpdateApiKeyRequest,
            body={"valid": valid},
            path={"apiKeyId": api_key_id},
        )

    async def a_update_organization_api_key(
        self, api_key_id: str, valid: bool
    ) -> ApiKey:
        """Update Organization API Key

        Activates or deactivates an organization-scoped API key. A deactivated
        key is rejected on authentication from the next request onwards; its
        value is unchanged and reactivating it brings the same value back.
        Rotating is the only way to change the value.

        Args:
            api_key_id: The id of the API key.
            valid: Send false to deactivate the key, true to reactivate it. A
                deactivated key is rejected on every request, and deactivating
                one takes effect immediately.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKey,
            request_schema=UpdateApiKeyRequest,
            body={"valid": valid},
            path={"apiKeyId": api_key_id},
        )

    def delete_organization_api_key(self, api_key_id: str) -> ApiKeyRef:
        """Revoke Organization API Key

        Permanently revokes an organization-scoped API key. Both its current
        value and any replacement value in flight stop authenticating at once.
        This cannot be undone — a new key has to be created in its place.

        Args:
            api_key_id: The id of the API key.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKeyRef,
            path={"apiKeyId": api_key_id},
        )

    async def a_delete_organization_api_key(self, api_key_id: str) -> ApiKeyRef:
        """Revoke Organization API Key

        Permanently revokes an organization-scoped API key. Both its current
        value and any replacement value in flight stop authenticating at once.
        This cannot be undone — a new key has to be created in its place.

        Args:
            api_key_id: The id of the API key.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.ORGANIZATION_API_KEY_ENDPOINT,
            response_schema=ApiKeyRef,
            path={"apiKeyId": api_key_id},
        )

    def rotate_organization_api_key(
        self,
        api_key_id: str,
        *,
        grace_period_in_hours: Optional[int] = None,
        expires_in_days: Optional[int] = None
    ) -> RotatedApiKey:
        """Rotate Organization API Key

        Rotates an organization-scoped API key in place — the key keeps its id,
        name and history, and no second key is created. The new value is
        returned **exactly once**, in this response, and can never be retrieved
        again — store it securely. With `gracePeriodInHours: 0` (the default)
        the key's `value` is replaced as this request is served and the outgoing
        value stops authenticating at once. With a grace period, the new value
        comes back as `shadowValue` and both values authenticate until
        `rotatesAt`, after which the new value becomes `value` and the outgoing
        one is rejected; requests made with the outgoing value in the meantime
        carry `Sunset` and `X-Api-Key-Warning` headers announcing when it stops
        working. The key's expiry is left alone unless `expiresInDays` is sent.
        Rotating an **expired** key revives it: `expiresInDays` is then required
        (send null for no expiry) and a grace period is not allowed. A rotation
        whose grace period has already run out is completed before this one
        starts.

        Args:
            api_key_id: The id of the API key.
            grace_period_in_hours: How long the current value keeps
                authenticating alongside the new one, in hours from now — a
                duration, not a date, stored on the key as `rotatesAt` and never
                set past `expiresAt`. Defaults to 0, which replaces the value
                immediately and stops the old one at once.
            expires_in_days: A new lifetime for the key, in days from now — a
                duration, not a date, stored on the key as `expiresAt`. Omit it
                to keep the current expiry, or send null to remove the expiry
                altogether. Required when rotating a key that has already
                expired.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_API_KEY_ROTATE_ENDPOINT,
            response_schema=RotatedApiKey,
            request_schema=RotateApiKeyRequest,
            body={
                "gracePeriodInHours": grace_period_in_hours,
                "expiresInDays": expires_in_days,
            },
            path={"apiKeyId": api_key_id},
        )

    async def a_rotate_organization_api_key(
        self,
        api_key_id: str,
        *,
        grace_period_in_hours: Optional[int] = None,
        expires_in_days: Optional[int] = None
    ) -> RotatedApiKey:
        """Rotate Organization API Key

        Rotates an organization-scoped API key in place — the key keeps its id,
        name and history, and no second key is created. The new value is
        returned **exactly once**, in this response, and can never be retrieved
        again — store it securely. With `gracePeriodInHours: 0` (the default)
        the key's `value` is replaced as this request is served and the outgoing
        value stops authenticating at once. With a grace period, the new value
        comes back as `shadowValue` and both values authenticate until
        `rotatesAt`, after which the new value becomes `value` and the outgoing
        one is rejected; requests made with the outgoing value in the meantime
        carry `Sunset` and `X-Api-Key-Warning` headers announcing when it stops
        working. The key's expiry is left alone unless `expiresInDays` is sent.
        Rotating an **expired** key revives it: `expiresInDays` is then required
        (send null for no expiry) and a grace period is not allowed. A rotation
        whose grace period has already run out is completed before this one
        starts.

        Args:
            api_key_id: The id of the API key.
            grace_period_in_hours: How long the current value keeps
                authenticating alongside the new one, in hours from now — a
                duration, not a date, stored on the key as `rotatesAt` and never
                set past `expiresAt`. Defaults to 0, which replaces the value
                immediately and stops the old one at once.
            expires_in_days: A new lifetime for the key, in days from now — a
                duration, not a date, stored on the key as `expiresAt`. Omit it
                to keep the current expiry, or send null to remove the expiry
                altogether. Required when rotating a key that has already
                expired.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.ORGANIZATION_API_KEY_ROTATE_ENDPOINT,
            response_schema=RotatedApiKey,
            request_schema=RotateApiKeyRequest,
            body={
                "gracePeriodInHours": grace_period_in_hours,
                "expiresInDays": expires_in_days,
            },
            path={"apiKeyId": api_key_id},
        )

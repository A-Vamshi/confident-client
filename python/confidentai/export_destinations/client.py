# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: export-destinations).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.endpoints import Endpoints
from confidentai.export_destinations.types import (
    CreateExportDestinationRequest,
    ExportDestination,
    ExportDestinationList,
    ExportDestinationRef,
    ExportDestinationType,
    UpdateExportDestinationRequest,
)


class ExportDestinationsClient:
    def __init__(self, api: Api) -> None:
        self._api = api

    def list_export_destinations(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> ExportDestinationList:
        """List Export Destinations

        Lists the export destinations in your Confident AI project one page at a
        time, newest first. Each destination is returned without its
        credentials; retrieve one by id to see them in masked form.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.EXPORT_DESTINATIONS_ENDPOINT,
            response_schema=ExportDestinationList,
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_export_destinations(
        self, *, page: Optional[int] = None, page_size: Optional[int] = None
    ) -> ExportDestinationList:
        """List Export Destinations

        Lists the export destinations in your Confident AI project one page at a
        time, newest first. Each destination is returned without its
        credentials; retrieve one by id to see them in masked form.

        Args:
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.EXPORT_DESTINATIONS_ENDPOINT,
            response_schema=ExportDestinationList,
            query={"page": page, "pageSize": page_size},
        )

    def create_export_destination(
        self,
        name: str,
        bucket: str,
        region: str,
        access_key_id: str,
        secret_access_key: str,
        *,
        type: Optional[ExportDestinationType] = None,
        path_prefix: Optional[str] = None,
        enabled: Optional[bool] = None
    ) -> ExportDestinationRef:
        """Create Export Destination

        Creates an export destination in your Confident AI project and returns
        its id. Credentials must be sent in full — a masked value copied from a
        read is rejected, since storing the mask would leave a destination that
        can never authenticate. A project holds at most three destinations.

        Args:
            name: The name of the destination, as it appears in your project.
            bucket: The name of the bucket exports are uploaded to.
            region: The region the bucket lives in.
            access_key_id: The access key id Confident AI uploads with. Reads
                return this masked, as fifteen asterisks followed by its last
                six characters, so a masked value is rejected here — send the
                real key id.
            secret_access_key: The secret access key paired with `accessKeyId`.
                Confident AI never returns it in full, so a masked value is
                rejected here — send the real secret.
            path_prefix: A folder inside the bucket to write exports under. A
                leading slash is stripped and a trailing one added, so
                `/confident-ai` is stored as `confident-ai/`. Omit it, or send
                null or an empty string, to write to the root of the bucket.
            enabled: Whether export schedules may upload to this destination.
                Defaults to true.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.EXPORT_DESTINATIONS_ENDPOINT,
            response_schema=ExportDestinationRef,
            request_schema=CreateExportDestinationRequest,
            body={
                "name": name,
                "bucket": bucket,
                "region": region,
                "accessKeyId": access_key_id,
                "secretAccessKey": secret_access_key,
                "type": type,
                "pathPrefix": path_prefix,
                "enabled": enabled,
            },
        )

    async def a_create_export_destination(
        self,
        name: str,
        bucket: str,
        region: str,
        access_key_id: str,
        secret_access_key: str,
        *,
        type: Optional[ExportDestinationType] = None,
        path_prefix: Optional[str] = None,
        enabled: Optional[bool] = None
    ) -> ExportDestinationRef:
        """Create Export Destination

        Creates an export destination in your Confident AI project and returns
        its id. Credentials must be sent in full — a masked value copied from a
        read is rejected, since storing the mask would leave a destination that
        can never authenticate. A project holds at most three destinations.

        Args:
            name: The name of the destination, as it appears in your project.
            bucket: The name of the bucket exports are uploaded to.
            region: The region the bucket lives in.
            access_key_id: The access key id Confident AI uploads with. Reads
                return this masked, as fifteen asterisks followed by its last
                six characters, so a masked value is rejected here — send the
                real key id.
            secret_access_key: The secret access key paired with `accessKeyId`.
                Confident AI never returns it in full, so a masked value is
                rejected here — send the real secret.
            path_prefix: A folder inside the bucket to write exports under. A
                leading slash is stripped and a trailing one added, so
                `/confident-ai` is stored as `confident-ai/`. Omit it, or send
                null or an empty string, to write to the root of the bucket.
            enabled: Whether export schedules may upload to this destination.
                Defaults to true.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.EXPORT_DESTINATIONS_ENDPOINT,
            response_schema=ExportDestinationRef,
            request_schema=CreateExportDestinationRequest,
            body={
                "name": name,
                "bucket": bucket,
                "region": region,
                "accessKeyId": access_key_id,
                "secretAccessKey": secret_access_key,
                "type": type,
                "pathPrefix": path_prefix,
                "enabled": enabled,
            },
        )

    def get_export_destination(
        self, export_destination_id: str
    ) -> ExportDestination:
        """Get Export Destination

        Retrieves an export destination by id. Its credentials come back masked
        — fifteen asterisks followed by the last six characters of the stored
        value — so you can tell which key is in use without the key itself being
        returned.

        Args:
            export_destination_id: The id of the export destination.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestination,
            path={"exportDestinationId": export_destination_id},
        )

    async def a_get_export_destination(
        self, export_destination_id: str
    ) -> ExportDestination:
        """Get Export Destination

        Retrieves an export destination by id. Its credentials come back masked
        — fifteen asterisks followed by the last six characters of the stored
        value — so you can tell which key is in use without the key itself being
        returned.

        Args:
            export_destination_id: The id of the export destination.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestination,
            path={"exportDestinationId": export_destination_id},
        )

    def update_export_destination(
        self,
        export_destination_id: str,
        *,
        name: Optional[str] = None,
        type: Optional[ExportDestinationType] = None,
        bucket: Optional[str] = None,
        region: Optional[str] = None,
        access_key_id: Optional[str] = None,
        secret_access_key: Optional[str] = None,
        path_prefix: Optional[str] = None,
        enabled: Optional[bool] = None
    ) -> ExportDestination:
        """Update Export Destination

        Updates an export destination and returns it with its credentials
        masked. Omit a credential to leave the stored one alone; resending the
        masked value a read returned does the same, so a destination you read
        and send straight back keeps working.

        Args:
            export_destination_id: The id of the export destination.
            name: The name of the destination, as it appears in your project.
            bucket: The name of the bucket exports are uploaded to.
            region: The region the bucket lives in.
            access_key_id: A new access key id for the bucket. Omit it to leave
                the stored key id untouched. Sending back the masked value a
                read returned leaves it untouched too, so a destination you read
                and send straight back keeps working.
            secret_access_key: A new secret access key for the bucket. Omit it
                to leave the stored secret untouched. Sending back the masked
                value a read returned leaves it untouched too; the mask is never
                written into storage.
            path_prefix: A folder inside the bucket to write exports under. A
                leading slash is stripped and a trailing one added, so
                `/confident-ai` is stored as `confident-ai/`. Send null to clear
                it and write to the root of the bucket; an empty string is
                ignored.
            enabled: Whether export schedules may upload to this destination.
                Set it to false to stop uploads without deleting the
                destination.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestination,
            request_schema=UpdateExportDestinationRequest,
            body={
                "name": name,
                "type": type,
                "bucket": bucket,
                "region": region,
                "accessKeyId": access_key_id,
                "secretAccessKey": secret_access_key,
                "pathPrefix": path_prefix,
                "enabled": enabled,
            },
            path={"exportDestinationId": export_destination_id},
        )

    async def a_update_export_destination(
        self,
        export_destination_id: str,
        *,
        name: Optional[str] = None,
        type: Optional[ExportDestinationType] = None,
        bucket: Optional[str] = None,
        region: Optional[str] = None,
        access_key_id: Optional[str] = None,
        secret_access_key: Optional[str] = None,
        path_prefix: Optional[str] = None,
        enabled: Optional[bool] = None
    ) -> ExportDestination:
        """Update Export Destination

        Updates an export destination and returns it with its credentials
        masked. Omit a credential to leave the stored one alone; resending the
        masked value a read returned does the same, so a destination you read
        and send straight back keeps working.

        Args:
            export_destination_id: The id of the export destination.
            name: The name of the destination, as it appears in your project.
            bucket: The name of the bucket exports are uploaded to.
            region: The region the bucket lives in.
            access_key_id: A new access key id for the bucket. Omit it to leave
                the stored key id untouched. Sending back the masked value a
                read returned leaves it untouched too, so a destination you read
                and send straight back keeps working.
            secret_access_key: A new secret access key for the bucket. Omit it
                to leave the stored secret untouched. Sending back the masked
                value a read returned leaves it untouched too; the mask is never
                written into storage.
            path_prefix: A folder inside the bucket to write exports under. A
                leading slash is stripped and a trailing one added, so
                `/confident-ai` is stored as `confident-ai/`. Send null to clear
                it and write to the root of the bucket; an empty string is
                ignored.
            enabled: Whether export schedules may upload to this destination.
                Set it to false to stop uploads without deleting the
                destination.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestination,
            request_schema=UpdateExportDestinationRequest,
            body={
                "name": name,
                "type": type,
                "bucket": bucket,
                "region": region,
                "accessKeyId": access_key_id,
                "secretAccessKey": secret_access_key,
                "pathPrefix": path_prefix,
                "enabled": enabled,
            },
            path={"exportDestinationId": export_destination_id},
        )

    def delete_export_destination(
        self, export_destination_id: str
    ) -> ExportDestinationRef:
        """Delete Export Destination

        Permanently deletes an export destination. Every export schedule
        pointing at it is disabled first, so no schedule is left silently
        falling back to another delivery mode.

        Args:
            export_destination_id: The id of the export destination.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestinationRef,
            path={"exportDestinationId": export_destination_id},
        )

    async def a_delete_export_destination(
        self, export_destination_id: str
    ) -> ExportDestinationRef:
        """Delete Export Destination

        Permanently deletes an export destination. Every export schedule
        pointing at it is disabled first, so no schedule is left silently
        falling back to another delivery mode.

        Args:
            export_destination_id: The id of the export destination.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.EXPORT_DESTINATION_ENDPOINT,
            response_schema=ExportDestinationRef,
            path={"exportDestinationId": export_destination_id},
        )

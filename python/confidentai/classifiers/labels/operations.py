# @generated by scripts/generate_sdk.py from
# packages/shared/openapi/openapi.yml (tag: classifiers).
# Do not edit by hand — change the route in confident-cloud
# and regenerate.

from typing import Optional

from confidentai.api import Api, HttpMethods
from confidentai.classifiers.types import (
    ClassifierLabel,
    ClassifierLabelList,
    ClassifierLabelRef,
    ClassifierLabelStatus,
    CreateClassifierLabelRequest,
    SignalPolarity,
    UpdateClassifierLabelRequest,
)
from confidentai.endpoints import Endpoints


class LabelsOperations:
    _api: Api

    def list_labels(
        self,
        classifier_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> ClassifierLabelList:
        """List Classifier Labels

        Lists a classifier's labels one page at a time, ordered by name. This is
        also how you read the results of a generation run — generated
        suggestions arrive with status RECOMMENDED. Each label is returned as a
        summary row; retrieve one by id for its description and polarity.
        Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier.
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.CLASSIFIER_LABELS_ENDPOINT,
            response_schema=ClassifierLabelList,
            path={"classifierId": classifier_id},
            query={"page": page, "pageSize": page_size},
        )

    async def a_list_labels(
        self,
        classifier_id: str,
        *,
        page: Optional[int] = None,
        page_size: Optional[int] = None
    ) -> ClassifierLabelList:
        """List Classifier Labels

        Lists a classifier's labels one page at a time, ordered by name. This is
        also how you read the results of a generation run — generated
        suggestions arrive with status RECOMMENDED. Each label is returned as a
        summary row; retrieve one by id for its description and polarity.
        Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier.
            page: The page to return. Defaults to 1.
            page_size: The number of results per page, at most 100. Defaults to
                25.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.CLASSIFIER_LABELS_ENDPOINT,
            response_schema=ClassifierLabelList,
            path={"classifierId": classifier_id},
            query={"page": page, "pageSize": page_size},
        )

    def create_label(
        self,
        classifier_id: str,
        name: str,
        description: str,
        *,
        enabled: Optional[bool] = None,
        status: Optional[ClassifierLabelStatus] = None,
        polarity: Optional[SignalPolarity] = None
    ) -> ClassifierLabelRef:
        """Create Classifier Label

        Adds a label to a classifier and returns its id. The `description` is
        what the classifying model matches against, so write it as a clear
        statement of when the label applies rather than a restatement of the
        name. Label names are unique within a classifier. Requires the Starter
        plan or above.

        Args:
            classifier_id: The id of the classifier.
            name: The name of the label, unique within the classifier.
            description: When this label applies. It is the instruction the
                classifying model reads, so state the condition rather than
                restating the name.
            enabled: Whether the label can be applied. Defaults to true.
        """
        return self._api.request(
            HttpMethods.POST,
            Endpoints.CLASSIFIER_LABELS_ENDPOINT,
            response_schema=ClassifierLabelRef,
            request_schema=CreateClassifierLabelRequest,
            body={
                "name": name,
                "description": description,
                "enabled": enabled,
                "status": status,
                "polarity": polarity,
            },
            path={"classifierId": classifier_id},
        )

    async def a_create_label(
        self,
        classifier_id: str,
        name: str,
        description: str,
        *,
        enabled: Optional[bool] = None,
        status: Optional[ClassifierLabelStatus] = None,
        polarity: Optional[SignalPolarity] = None
    ) -> ClassifierLabelRef:
        """Create Classifier Label

        Adds a label to a classifier and returns its id. The `description` is
        what the classifying model matches against, so write it as a clear
        statement of when the label applies rather than a restatement of the
        name. Label names are unique within a classifier. Requires the Starter
        plan or above.

        Args:
            classifier_id: The id of the classifier.
            name: The name of the label, unique within the classifier.
            description: When this label applies. It is the instruction the
                classifying model reads, so state the condition rather than
                restating the name.
            enabled: Whether the label can be applied. Defaults to true.
        """
        return await self._api.a_request(
            HttpMethods.POST,
            Endpoints.CLASSIFIER_LABELS_ENDPOINT,
            response_schema=ClassifierLabelRef,
            request_schema=CreateClassifierLabelRequest,
            body={
                "name": name,
                "description": description,
                "enabled": enabled,
                "status": status,
                "polarity": polarity,
            },
            path={"classifierId": classifier_id},
        )

    def get_label(self, classifier_id: str, label_id: str) -> ClassifierLabel:
        """Get Classifier Label

        Retrieves a single label on a classifier, with the description the
        classifying model matches against and the polarity trend reporting uses.
        Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
        """
        return self._api.request(
            HttpMethods.GET,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabel,
            path={"classifierId": classifier_id, "labelId": label_id},
        )

    async def a_get_label(
        self, classifier_id: str, label_id: str
    ) -> ClassifierLabel:
        """Get Classifier Label

        Retrieves a single label on a classifier, with the description the
        classifying model matches against and the polarity trend reporting uses.
        Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
        """
        return await self._api.a_request(
            HttpMethods.GET,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabel,
            path={"classifierId": classifier_id, "labelId": label_id},
        )

    def update_label(
        self,
        classifier_id: str,
        label_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        status: Optional[ClassifierLabelStatus] = None,
        polarity: Optional[SignalPolarity] = None
    ) -> ClassifierLabel:
        """Update Classifier Label

        Updates a label on a classifier and returns it. Only the fields you send
        are changed. Promoting a generated suggestion is an update to status
        ACTIVE, which also enables the label. Requires the Starter plan or
        above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
            name: The name of the label, unique within the classifier.
            description: When this label applies. It cannot be cleared.
            enabled: Whether the label can be applied. Defaults to true.
        """
        return self._api.request(
            HttpMethods.PUT,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabel,
            request_schema=UpdateClassifierLabelRequest,
            body={
                "name": name,
                "description": description,
                "enabled": enabled,
                "status": status,
                "polarity": polarity,
            },
            path={"classifierId": classifier_id, "labelId": label_id},
        )

    async def a_update_label(
        self,
        classifier_id: str,
        label_id: str,
        *,
        name: Optional[str] = None,
        description: Optional[str] = None,
        enabled: Optional[bool] = None,
        status: Optional[ClassifierLabelStatus] = None,
        polarity: Optional[SignalPolarity] = None
    ) -> ClassifierLabel:
        """Update Classifier Label

        Updates a label on a classifier and returns it. Only the fields you send
        are changed. Promoting a generated suggestion is an update to status
        ACTIVE, which also enables the label. Requires the Starter plan or
        above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
            name: The name of the label, unique within the classifier.
            description: When this label applies. It cannot be cleared.
            enabled: Whether the label can be applied. Defaults to true.
        """
        return await self._api.a_request(
            HttpMethods.PUT,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabel,
            request_schema=UpdateClassifierLabelRequest,
            body={
                "name": name,
                "description": description,
                "enabled": enabled,
                "status": status,
                "polarity": polarity,
            },
            path={"classifierId": classifier_id, "labelId": label_id},
        )

    def delete_label(
        self, classifier_id: str, label_id: str
    ) -> ClassifierLabelRef:
        """Delete Classifier Label

        Permanently deletes a label from a classifier and returns its id.
        Classifications already applied with it are not removed. This action
        cannot be undone. Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
        """
        return self._api.request(
            HttpMethods.DELETE,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabelRef,
            path={"classifierId": classifier_id, "labelId": label_id},
        )

    async def a_delete_label(
        self, classifier_id: str, label_id: str
    ) -> ClassifierLabelRef:
        """Delete Classifier Label

        Permanently deletes a label from a classifier and returns its id.
        Classifications already applied with it are not removed. This action
        cannot be undone. Requires the Starter plan or above.

        Args:
            classifier_id: The id of the classifier the label belongs to.
            label_id: The id of the label.
        """
        return await self._api.a_request(
            HttpMethods.DELETE,
            Endpoints.CLASSIFIER_LABEL_ENDPOINT,
            response_schema=ClassifierLabelRef,
            path={"classifierId": classifier_id, "labelId": label_id},
        )
